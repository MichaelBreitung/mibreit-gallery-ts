(function(e, a) { for(var i in a) e[i] = a[i]; if(a.__esModule) Object.defineProperty(e, "__esModule", { value: true }); }(exports,
/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 986:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_Fullscreen {\r\n  position: fixed;\r\n  box-sizing: border-box;\r\n  background-color: #181818;\r\n  overflow: hidden;\r\n  top: 0;\r\n  padding: 1rem;\r\n  width: 100%;\r\n  height: 100%;\r\n  z-index: 100;\r\n  display: flex;\r\n  flex-direction: column;\r\n}\r\n\r\n.mibreit_Fullscreen > .mibreit_Fullscreen_exit {\r\n  position: absolute;\r\n  width: 3rem;\r\n  height: 3rem;\r\n  right: 1rem;\r\n  top: 1rem;\r\n  z-index: 3;\r\n}\r\n\r\n.mibreit_Fullscreen > .mibreit_Fullscreen_exit > svg {\r\n  opacity: 0.75;\r\n}", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_Fullscreen": "mibreit_Fullscreen",
	"mibreit_Fullscreen_exit": "mibreit_Fullscreen_exit"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 861:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_GalleryFullscreenButton {\r\n  position: absolute;\r\n  z-index: 2;\r\n  width: 2rem;\r\n  height: 2rem;\r\n  right: 0.25rem;\r\n  top: 0.25rem;\r\n  opacity: 0;\r\n}\r\n\r\n.mibreit_GalleryPrevious {\r\n  position: absolute;\r\n  z-index: 2;\r\n  top: 50%;\r\n  opacity: 0;\r\n}\r\n\r\n.mibreit_GalleryNext {\r\n  position: absolute;\r\n  z-index: 2;\r\n  top: 50%;\r\n  right: 0rem;\r\n  opacity: 0;\r\n}\r\n\r\n.mibreit_GalleryNext > svg {\r\n  width: 3rem;\r\n  height: 3rem;\r\n  margin-top: -1.5rem;\r\n  fill: #727f88;\r\n}\r\n\r\n.mibreit_GalleryPrevious > svg {\r\n  transform: scaleX(-1);\r\n  width: 3rem;\r\n  height: 3rem;\r\n  margin-top: -1.5rem;  \r\n  fill: #727f88;\r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_GalleryFullscreenButton": "mibreit_GalleryFullscreenButton",
	"mibreit_GalleryPrevious": "mibreit_GalleryPrevious",
	"mibreit_GalleryNext": "mibreit_GalleryNext"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 706:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_HorizontalScroller {\r\n  height: 100%;\r\n  display: flex;\r\n  flex-direction: row;\r\n  align-items: center;\r\n  -webkit-transition: transform 600ms ease-in-out;\r\n  -moz-transition: transform 600ms ease-in-out;\r\n  -o-transition: transform 600ms ease-in-out;\r\n  transition: transform 600ms ease-in-out;\r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_HorizontalScroller": "mibreit_HorizontalScroller"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 710:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_ImageStage {\r\n  position: absolute;\r\n  top: 0;\r\n  left: 0;\r\n  width: 100%;\r\n  height: 100%;\r\n  opacity: 0;\r\n  z-index: 0;\r\n  overflow: hidden;\r\n}\r\n\r\n.mibreit_ImageStage > img.zoom {\r\n  -webkit-transition: transform 10000ms linear;\r\n  -moz-transition: transform 10000ms linear;\r\n  -o-transition: transform 10000ms linear;\r\n  transition: transform 10000ms linear;\r\n  -webkit-transform: scale(1.125);\r\n  -moz-transform: scale(1.125);\r\n  -o-transform: scale(1.125);\r\n  transform: scale(1.125);\r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ImageStage": "mibreit_ImageStage",
	"zoom": "zoom"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 136:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_ThumbScrollerParentContainer {\r\n  display: flex;\r\n  flex-direction: row;\r\n  align-items: center;\r\n}\r\n\r\n.mibreit_ThumbScrollerContainer {\r\n  height: 100%;\r\n  width: 100%;\r\n  overflow: hidden;\r\n}\r\n\r\n.mibreit_ThumbScrollerContainerCentered {\r\n  height: 100%;\r\n  width: 100%;\r\n  overflow: hidden;\r\n  display: flex;\r\n  justify-content: center;\r\n  align-items: center;\r\n}\r\n\r\n.mibreit_ThumbScrollerParentContainer svg {\r\n  width: 3rem;\r\n  height: 3rem;\r\n  fill: #727f88;\r\n}\r\n\r\n.mibreit_ThumbScrollerPrevious,\r\n.mibreit_ThumbScrollerNext {\r\n  margin-right: 0.5rem;\r\n  margin-left: 0.5rem;\r\n}\r\n\r\n.mibreit_ThumbScrollerPrevious > svg {\r\n  transform: scaleX(-1);\r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ThumbScrollerParentContainer": "mibreit_ThumbScrollerParentContainer",
	"mibreit_ThumbScrollerContainer": "mibreit_ThumbScrollerContainer",
	"mibreit_ThumbScrollerContainerCentered": "mibreit_ThumbScrollerContainerCentered",
	"mibreit_ThumbScrollerPrevious": "mibreit_ThumbScrollerPrevious",
	"mibreit_ThumbScrollerNext": "mibreit_ThumbScrollerNext"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 867:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_ThumbStage {\r\n  margin: 0.25rem;\r\n  position: relative;\r\n  flex-shrink: 0;\r\n}\r\n\r\n.mibreit_ThumbStage:hover {\r\n  transform: scale(1.1);\r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ThumbStage": "mibreit_ThumbStage"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 614:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

"use strict";
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "Z": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".mibreit_GalleryFade\r\n{\r\n  -webkit-transition: opacity 800ms ease-out;\r\n  -moz-transition: opacity 800ms ease-out;\r\n  -o-transition: opacity 800ms ease-out;\r\n  transition: opacity 800ms ease-out;\r\n}", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_GalleryFade": "mibreit_GalleryFade"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 645:
/***/ ((module) => {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (cssWithMappingToString) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join('');
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === 'string') {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, '']];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

/***/ }),

/***/ 942:
/***/ ((__unused_webpack_module, exports) => {

(function(e, a) { for(var i in a) e[i] = a[i]; if(a.__esModule) Object.defineProperty(e, "__esModule", { value: true }); }(exports,
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ 607:
/***/ ((__unused_webpack_module, exports) => {


/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DomTools = void 0;
function documentReady(callback) {
    window.addEventListener('load', callback);
}
function getRootFontSize() {
    return parseFloat(window.getComputedStyle(document.body).getPropertyValue('font-size'));
}
function createElement(tagName) {
    return document.createElement(tagName);
}
function removeElement(element) {
    element.parentElement.removeChild(element);
}
function prependChildElement(element, parent) {
    parent.prepend(element);
}
function appendChildElement(element, parent) {
    parent.append(element);
}
function prependBeforeChild(element, child) {
    child.parentElement.insertBefore(element, child);
}
function getChildNodes(element) {
    var nodes = element.childNodes;
    var nodesArray = new Array();
    for (var i = 0; i < nodes.length; ++i) {
        nodesArray.push(nodes[i]);
    }
    return nodesArray;
}
function setInnerHtml(parent, inner) {
    parent.innerHTML = inner;
}
function wrapElements(elements, wrapper) {
    elements[0].parentNode.insertBefore(wrapper, elements[0]);
    elements.forEach(function (element) {
        wrapper.appendChild(element);
    });
}
function unwrapElements(wrapper) {
    var elements = wrapper.childNodes;
    elements.forEach(function (element) {
        wrapper.parentNode.insertBefore(element, wrapper);
    });
}
function getParentElement(element) {
    return element.parentElement;
}
function getElementDimension(element) {
    return {
        width: element.clientWidth,
        height: element.clientHeight,
    };
}
function isElementWithinWindow(element) {
    var elementRect = element.getBoundingClientRect();
    var windowHeight = window.innerHeight;
    return elementRect.top + elementRect.height > 0 && elementRect.top < windowHeight;
}
function getElementPosition(element) {
    var rect = element.getBoundingClientRect();
    var scrollLeft = window.pageXOffset || document.documentElement.scrollLeft;
    var scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    return { y: rect.top + scrollTop, x: rect.left + scrollLeft };
}
function getCssClasses(element) {
    return getAttribute(element, 'class');
}
function addCssClass(element, cssClass) {
    if (!element.classList.contains(cssClass)) {
        element.classList.add(cssClass);
    }
}
function removeCssClass(element, cssClass) {
    if (element.classList.contains(cssClass)) {
        element.classList.remove(cssClass);
    }
}
function overwriteCssClasses(element, cssClasses) {
    element.setAttribute('class', cssClasses);
}
function removeAllCssClasses(element) {
    if (element.hasAttribute('class')) {
        element.removeAttribute('class');
    }
}
function getCssStyle(element, styleName) {
    return element.style.getPropertyValue(styleName);
}
function getComputedCssStyle(element, styleName) {
    return window.getComputedStyle(element).getPropertyValue(styleName);
}
function addCssStyle(element, styleName, styleProperty) {
    element.style.setProperty(styleName, styleProperty);
}
function removeCssStyle(element, styleName) {
    element.style.removeProperty(styleName);
    if (element.style.length === 0) {
        element.removeAttribute('style');
    }
}
function overwriteCssStyles(element, styles) {
    if (styles !== null) {
        element.style.cssText = styles;
    }
    else {
        element.removeAttribute('style');
    }
}
function hasAttribute(element, attribute) {
    return element.hasAttribute(attribute);
}
function getAttribute(element, attribute) {
    if (!element.hasAttribute(attribute)) {
        return null;
    }
    else {
        return element.getAttribute(attribute);
    }
}
function setAttribute(element, attribute, value) {
    element.setAttribute(attribute, value);
}
function removeAttribute(element, attribute) {
    element.removeAttribute(attribute);
}
function addClickEventListener(element, callback) {
    element.addEventListener('click', callback);
}
function addKeyEventListener(callback) {
    document.addEventListener('keydown', callback);
}
function addScrollEventListener(callback) {
    document.addEventListener('scroll', callback);
}
function addResizeEventListener(callback) {
    window.addEventListener('resize', callback);
}
function addEventListener(element, event, callback) {
    element.addEventListener(event, callback);
}
function disableContextMenu(element) {
    element.addEventListener('contextmenu', function (e) {
        e.preventDefault();
    });
}
function disableDragging(element) {
    element.addEventListener('dragstart', function (e) {
        e.preventDefault();
    });
}
function getElement(selector) {
    return document.querySelector(selector);
}
function getElements(selector) {
    return document.querySelectorAll(selector);
}
function getKeyFromEvent(event) {
    return event.key;
}
exports.DomTools = {
    documentReady: documentReady,
    getRootFontSize: getRootFontSize,
    createElement: createElement,
    removeElement: removeElement,
    prependChildElement: prependChildElement,
    appendChildElement: appendChildElement,
    prependBeforeChild: prependBeforeChild,
    getChildNodes: getChildNodes,
    setInnerHtml: setInnerHtml,
    wrapElements: wrapElements,
    unwrapElements: unwrapElements,
    getParentElement: getParentElement,
    getElementDimension: getElementDimension,
    getElementPosition: getElementPosition,
    isElementWithinWindow: isElementWithinWindow,
    getCssClasses: getCssClasses,
    addCssClass: addCssClass,
    removeCssClass: removeCssClass,
    removeAllCssClasses: removeAllCssClasses,
    overwriteCssClasses: overwriteCssClasses,
    getCssStyle: getCssStyle,
    getComputedCssStyle: getComputedCssStyle,
    addCssStyle: addCssStyle,
    removeCssStyle: removeCssStyle,
    overwriteCssStyles: overwriteCssStyles,
    hasAttribute: hasAttribute,
    getAttribute: getAttribute,
    setAttribute: setAttribute,
    removeAttribute: removeAttribute,
    addClickEventListener: addClickEventListener,
    addKeyEventListener: addKeyEventListener,
    addScrollEventListener: addScrollEventListener,
    addResizeEventListener: addResizeEventListener,
    addEventListener: addEventListener,
    disableContextMenu: disableContextMenu,
    disableDragging: disableDragging,
    getElement: getElement,
    getElements: getElements,
    getKeyFromEvent: getKeyFromEvent,
};


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __nested_webpack_require_7272__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		if(__webpack_module_cache__[moduleId]) {
/******/ 			return __webpack_module_cache__[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __nested_webpack_require_7272__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	// module exports must be returned from runtime so entry inlining is disabled
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	return __nested_webpack_require_7272__(607);
/******/ })()

));

/***/ }),

/***/ 199:
/***/ ((__unused_webpack_module, exports) => {

(function(e, a) { for(var i in a) e[i] = a[i]; if(a.__esModule) Object.defineProperty(e, "__esModule", { value: true }); }(exports,
/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 311:
/***/ ((module, __webpack_exports__, __nested_webpack_require_263__) => {

"use strict";
/* harmony export */ __nested_webpack_require_263__.d(__webpack_exports__, {
/* harmony export */   "Z": () => __WEBPACK_DEFAULT_EXPORT__
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__ = __nested_webpack_require_263__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__nested_webpack_require_263__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0__);
// Imports

var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_0___default()(function(i){return i[1]});
// Module
___CSS_LOADER_EXPORT___.push([module.id, ".element_animate_3pV {    \r\n  -webkit-transition: opacity 800ms ease-out;\r\n  -moz-transition: opacity 800ms ease-out;\r\n  -o-transition: opacity 800ms ease-out;\r\n  transition: opacity 800ms ease-out;  \r\n}\r\n", ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"element_animate": "element_animate_3pV"
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 645:
/***/ ((module) => {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
// css base code, injected by the css-loader
// eslint-disable-next-line func-names
module.exports = function (cssWithMappingToString) {
  var list = []; // return the list of modules as css string

  list.toString = function toString() {
    return this.map(function (item) {
      var content = cssWithMappingToString(item);

      if (item[2]) {
        return "@media ".concat(item[2], " {").concat(content, "}");
      }

      return content;
    }).join('');
  }; // import a list of modules into the list
  // eslint-disable-next-line func-names


  list.i = function (modules, mediaQuery, dedupe) {
    if (typeof modules === 'string') {
      // eslint-disable-next-line no-param-reassign
      modules = [[null, modules, '']];
    }

    var alreadyImportedModules = {};

    if (dedupe) {
      for (var i = 0; i < this.length; i++) {
        // eslint-disable-next-line prefer-destructuring
        var id = this[i][0];

        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }

    for (var _i = 0; _i < modules.length; _i++) {
      var item = [].concat(modules[_i]);

      if (dedupe && alreadyImportedModules[item[0]]) {
        // eslint-disable-next-line no-continue
        continue;
      }

      if (mediaQuery) {
        if (!item[2]) {
          item[2] = mediaQuery;
        } else {
          item[2] = "".concat(mediaQuery, " and ").concat(item[2]);
        }
      }

      list.push(item);
    }
  };

  return list;
};

/***/ }),

/***/ 942:
/***/ ((__unused_webpack_module, exports) => {

(function(e, a) { for(var i in a) e[i] = a[i]; if(a.__esModule) Object.defineProperty(e, "__esModule", { value: true }); }(exports,
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ 607:
/***/ ((__unused_webpack_module, exports) => {


/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.DomTools = void 0;
function documentReady(callback) {
    window.addEventListener('load', callback);
}
function getRootFontSize() {
    return parseFloat(window.getComputedStyle(document.body).getPropertyValue('font-size'));
}
function createElement(tagName) {
    return document.createElement(tagName);
}
function removeElement(element) {
    element.parentElement.removeChild(element);
}
function prependChildElement(element, parent) {
    parent.prepend(element);
}
function appendChildElement(element, parent) {
    parent.append(element);
}
function prependBeforeChild(element, child) {
    child.parentElement.insertBefore(element, child);
}
function getChildNodes(element) {
    var nodes = element.childNodes;
    var nodesArray = new Array();
    for (var i = 0; i < nodes.length; ++i) {
        nodesArray.push(nodes[i]);
    }
    return nodesArray;
}
function setInnerHtml(parent, inner) {
    parent.innerHTML = inner;
}
function wrapElements(elements, wrapper) {
    elements[0].parentNode.insertBefore(wrapper, elements[0]);
    elements.forEach(function (element) {
        wrapper.appendChild(element);
    });
}
function unwrapElements(wrapper) {
    var elements = wrapper.childNodes;
    elements.forEach(function (element) {
        wrapper.parentNode.insertBefore(element, wrapper);
    });
}
function getParentElement(element) {
    return element.parentElement;
}
function getElementDimension(element) {
    return {
        width: element.clientWidth,
        height: element.clientHeight,
    };
}
function isElementWithinWindow(element) {
    var elementRect = element.getBoundingClientRect();
    var windowHeight = window.innerHeight;
    return elementRect.top + elementRect.height > 0 && elementRect.top < windowHeight;
}
function getElementPosition(element) {
    var rect = element.getBoundingClientRect();
    var scrollLeft = window.pageXOffset || document.documentElement.scrollLeft;
    var scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    return { y: rect.top + scrollTop, x: rect.left + scrollLeft };
}
function getCssClasses(element) {
    return getAttribute(element, 'class');
}
function addCssClass(element, cssClass) {
    if (!element.classList.contains(cssClass)) {
        element.classList.add(cssClass);
    }
}
function removeCssClass(element, cssClass) {
    if (element.classList.contains(cssClass)) {
        element.classList.remove(cssClass);
    }
}
function overwriteCssClasses(element, cssClasses) {
    element.setAttribute('class', cssClasses);
}
function removeAllCssClasses(element) {
    if (element.hasAttribute('class')) {
        element.removeAttribute('class');
    }
}
function getCssStyle(element, styleName) {
    return element.style.getPropertyValue(styleName);
}
function addCssStyle(element, styleName, styleProperty) {
    element.style.setProperty(styleName, styleProperty);
}
function removeCssStyle(element, styleName) {
    element.style.removeProperty(styleName);
    if (element.style.length === 0) {
        element.removeAttribute('style');
    }
}
function overwriteCssStyles(element, styles) {
    if (styles !== null) {
        element.style.cssText = styles;
    }
    else {
        element.removeAttribute('style');
    }
}
function hasAttribute(element, attribute) {
    return element.hasAttribute(attribute);
}
function getAttribute(element, attribute) {
    if (!element.hasAttribute(attribute)) {
        return null;
    }
    else {
        return element.getAttribute(attribute);
    }
}
function setAttribute(element, attribute, value) {
    element.setAttribute(attribute, value);
}
function removeAttribute(element, attribute) {
    element.removeAttribute(attribute);
}
function addClickEventListener(element, callback) {
    element.addEventListener('click', callback);
}
function addKeyEventListener(callback) {
    document.addEventListener('keydown', callback);
}
function addScrollEventListener(callback) {
    document.addEventListener('scroll', callback);
}
function addResizeEventListener(callback) {
    window.addEventListener('resize', callback);
}
function addEventListener(element, event, callback) {
    element.addEventListener(event, callback);
}
function disableContextMenu(element) {
    element.addEventListener('contextmenu', function (e) {
        e.preventDefault();
    });
}
function disableDragging(element) {
    element.addEventListener('dragstart', function (e) {
        e.preventDefault();
    });
}
function getElement(selector) {
    return document.querySelector(selector);
}
function getElements(selector) {
    return document.querySelectorAll(selector);
}
function getKeyFromEvent(event) {
    return event.key;
}
exports.DomTools = {
    documentReady: documentReady,
    getRootFontSize: getRootFontSize,
    createElement: createElement,
    removeElement: removeElement,
    prependChildElement: prependChildElement,
    appendChildElement: appendChildElement,
    prependBeforeChild: prependBeforeChild,
    getChildNodes: getChildNodes,
    setInnerHtml: setInnerHtml,
    wrapElements: wrapElements,
    unwrapElements: unwrapElements,
    getParentElement: getParentElement,
    getElementDimension: getElementDimension,
    getElementPosition: getElementPosition,
    isElementWithinWindow: isElementWithinWindow,
    getCssClasses: getCssClasses,
    addCssClass: addCssClass,
    removeCssClass: removeCssClass,
    removeAllCssClasses: removeAllCssClasses,
    overwriteCssClasses: overwriteCssClasses,
    getCssStyle: getCssStyle,
    addCssStyle: addCssStyle,
    removeCssStyle: removeCssStyle,
    overwriteCssStyles: overwriteCssStyles,
    hasAttribute: hasAttribute,
    getAttribute: getAttribute,
    setAttribute: setAttribute,
    removeAttribute: removeAttribute,
    addClickEventListener: addClickEventListener,
    addKeyEventListener: addKeyEventListener,
    addScrollEventListener: addScrollEventListener,
    addResizeEventListener: addResizeEventListener,
    addEventListener: addEventListener,
    disableContextMenu: disableContextMenu,
    disableDragging: disableDragging,
    getElement: getElement,
    getElements: getElements,
    getKeyFromEvent: getKeyFromEvent,
};


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __nested_webpack_require_7096__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		if(__webpack_module_cache__[moduleId]) {
/******/ 			return __webpack_module_cache__[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __nested_webpack_require_7096__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	// module exports must be returned from runtime so entry inlining is disabled
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	return __nested_webpack_require_7096__(607);
/******/ })()

));

/***/ }),

/***/ 233:
/***/ ((__unused_webpack_module, __webpack_exports__, __nested_webpack_require_11477__) => {

"use strict";
__nested_webpack_require_11477__.r(__webpack_exports__);
/* harmony export */ __nested_webpack_require_11477__.d(__webpack_exports__, {
/* harmony export */   "default": () => __WEBPACK_DEFAULT_EXPORT__
/* harmony export */ });
/* harmony import */ var _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__ = __nested_webpack_require_11477__(379);
/* harmony import */ var _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__nested_webpack_require_11477__.n(_node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_ElementLoader_module_css__WEBPACK_IMPORTED_MODULE_1__ = __nested_webpack_require_11477__(311);

            

var options = {"injectType":"singletonStyleTag"};

options.insert = "head";
options.singleton = true;

var update = _node_modules_style_loader_dist_runtime_injectStylesIntoStyleTag_js__WEBPACK_IMPORTED_MODULE_0___default()(_node_modules_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_ElementLoader_module_css__WEBPACK_IMPORTED_MODULE_1__/* .default */ .Z, options);



/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (_node_modules_css_loader_dist_cjs_js_ruleSet_1_rules_1_use_1_ElementLoader_module_css__WEBPACK_IMPORTED_MODULE_1__/* .default.locals */ .Z.locals || {});

/***/ }),

/***/ 379:
/***/ ((module, __unused_webpack_exports, __nested_webpack_require_12999__) => {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =   true ? __nested_webpack_require_12999__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ 371:
/***/ (function(__unused_webpack_module, exports, __nested_webpack_require_20171__) {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", ({ value: true }));
var ElementLoader_1 = __nested_webpack_require_20171__(990);
var ElementInfo_1 = __nested_webpack_require_20171__(429);
var Element = /** @class */ (function (_super) {
    __extends(Element, _super);
    function Element(element) {
        var _this = _super.call(this, element) || this;
        _this._elementInfo = new ElementInfo_1.default(_this._element);
        return _this;
    }
    Element.prototype.getHtmlElement = function () {
        return this._element;
    };
    Element.prototype.getWidth = function () {
        return this._elementInfo.getWidth();
    };
    Element.prototype.getHeight = function () {
        return this._elementInfo.getHeight();
    };
    Element.prototype.getStyle = function () {
        return this._elementInfo.getStyle();
    };
    Element.prototype.isElementWithinScrollArea = function () {
        return this._elementInfo.isElementWithinScrollArea();
    };
    return Element;
}(ElementLoader_1.default));
exports.default = Element;


/***/ }),

/***/ 429:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_22068__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
var mibreit_dom_tools_1 = __nested_webpack_require_22068__(942);
var ElementInfo = /** @class */ (function () {
    function ElementInfo(element) {
        this._element = element;
        var originalStyle = mibreit_dom_tools_1.DomTools.getAttribute(element, 'class');
        this._originalElementStyle = originalStyle ? originalStyle : '';
        this._width = parseInt(mibreit_dom_tools_1.DomTools.getAttribute(element, 'width'));
        this._height = parseInt(mibreit_dom_tools_1.DomTools.getAttribute(element, 'height'));
    }
    ElementInfo.prototype.getWidth = function () {
        return this._width;
    };
    ElementInfo.prototype.getHeight = function () {
        return this._height;
    };
    ElementInfo.prototype.getStyle = function () {
        return this._originalElementStyle;
    };
    ElementInfo.prototype.isElementWithinScrollArea = function () {
        return mibreit_dom_tools_1.DomTools.isElementWithinWindow(this._element);
    };
    return ElementInfo;
}());
exports.default = ElementInfo;


/***/ }),

/***/ 990:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_23399__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.SRC_ATTRIBUTE = exports.DATA_SRC_ATTRIBUTE = void 0;
var mibreit_dom_tools_1 = __nested_webpack_require_23399__(942);
var ElementLoader_module_css_1 = __nested_webpack_require_23399__(233);
exports.DATA_SRC_ATTRIBUTE = 'data-src';
exports.SRC_ATTRIBUTE = 'src';
var EImageState;
(function (EImageState) {
    EImageState[EImageState["INACTIVE"] = 0] = "INACTIVE";
    EImageState[EImageState["LOADING"] = 1] = "LOADING";
    EImageState[EImageState["LOADED"] = 2] = "LOADED";
})(EImageState || (EImageState = {}));
var ElementLoader = /** @class */ (function () {
    function ElementLoader(element) {
        this._state = EImageState.INACTIVE;
        this._wasLoadedCallbacks = new Array();
        this._element = element;
        if (!mibreit_dom_tools_1.DomTools.hasAttribute(element, exports.DATA_SRC_ATTRIBUTE)) {
            this._state = EImageState.LOADED;
        }
        else {
            this._state = EImageState.INACTIVE;
            this._setBaseStyle();
        }
    }
    ElementLoader.prototype.load = function () {
        var _this = this;
        return new Promise(function (resolve, reject) {
            if (_this._state === EImageState.INACTIVE) {
                _this._element.onload = function () {
                    mibreit_dom_tools_1.DomTools.removeAttribute(_this._element, exports.DATA_SRC_ATTRIBUTE);
                    _this._state = EImageState.LOADED;
                    _this._wasLoadedCallbacks.forEach(function (callback) {
                        callback();
                    });
                    // A little explanation for the timeout: we need to decouple the 
                    // change of the load style and the fade it will trigger from the
                    // change to the dom that might be triggered by the callbacks
                    // -> this needs to happen separately and a timeout helps to provide
                    // this separation
                    setTimeout(function () {
                        _this._setLoadedStyle();
                    }, 50);
                    resolve(true);
                };
                _this._state = EImageState.LOADING;
                _this._setLoadingStyle();
                var dataSrc = mibreit_dom_tools_1.DomTools.getAttribute(_this._element, exports.DATA_SRC_ATTRIBUTE);
                mibreit_dom_tools_1.DomTools.setAttribute(_this._element, exports.SRC_ATTRIBUTE, dataSrc);
            }
            else if (_this._state === EImageState.LOADING) {
                reject(false);
            }
            else {
                reject(true);
            }
        });
    };
    ElementLoader.prototype.wasLoaded = function () {
        return this._state === EImageState.LOADED;
    };
    ElementLoader.prototype.addWasLoadedCallback = function (callback) {
        if (!this._wasLoadedCallbacks.includes(callback)) {
            this._wasLoadedCallbacks.push(callback);
        }
    };
    ElementLoader.prototype._setBaseStyle = function () {
        mibreit_dom_tools_1.DomTools.addCssStyle(this._element, 'opacity', '0');
    };
    ElementLoader.prototype._setLoadingStyle = function () {
        mibreit_dom_tools_1.DomTools.addCssClass(this._element, ElementLoader_module_css_1.default.element_animate);
    };
    ElementLoader.prototype._setLoadedStyle = function () {
        var _this = this;
        mibreit_dom_tools_1.DomTools.removeCssStyle(this._element, 'opacity');
        setTimeout(function () {
            mibreit_dom_tools_1.DomTools.removeCssClass(_this._element, ElementLoader_module_css_1.default.element_animate);
        }, 1000);
    };
    return ElementLoader;
}());
exports.default = ElementLoader;


/***/ }),

/***/ 924:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_27423__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
var mibreit_dom_tools_1 = __nested_webpack_require_27423__(942);
var ElementSurrogate = /** @class */ (function () {
    function ElementSurrogate(elementInfo) {
        this._elementInfo = elementInfo;
        this._surrogate = this._createSurrogate();
    }
    ElementSurrogate.prototype.wrap = function (elementHandle) {
        mibreit_dom_tools_1.DomTools.wrapElements([elementHandle], this._surrogate);
    };
    ElementSurrogate.prototype.unwrap = function () {
        mibreit_dom_tools_1.DomTools.unwrapElements(this._surrogate);
        mibreit_dom_tools_1.DomTools.removeElement(this._surrogate);
    };
    ElementSurrogate.prototype.getWidth = function () {
        return this._elementInfo.getWidth();
    };
    ElementSurrogate.prototype.getHeight = function () {
        return this._elementInfo.getHeight();
    };
    ElementSurrogate.prototype.getStyle = function () {
        return this._elementInfo.getStyle();
    };
    ElementSurrogate.prototype.isElementWithinScrollArea = function () {
        return mibreit_dom_tools_1.DomTools.isElementWithinWindow(this._surrogate);
    };
    ElementSurrogate.prototype._createSurrogate = function () {
        var surrogate = mibreit_dom_tools_1.DomTools.createElement('div');
        mibreit_dom_tools_1.DomTools.addCssClass(surrogate, 'mibreit_lazyLoader_surrogate');
        mibreit_dom_tools_1.DomTools.overwriteCssStyles(surrogate, "overflow: hidden; width: 100%; height: 0; padding-bottom: " + (this._elementInfo.getHeight() * 100) / this._elementInfo.getWidth() + "%;");
        return surrogate;
    };
    return ElementSurrogate;
}());
exports.default = ElementSurrogate;


/***/ }),

/***/ 304:
/***/ (function(__unused_webpack_module, exports) {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", ({ value: true }));
var PRELOADER_WINDOW_SIZE = 5;
var LazyLoader = /** @class */ (function () {
    function LazyLoader(elementLoaders, preloaderBeforeSize, preloaderAfterSize) {
        if (preloaderBeforeSize === void 0) { preloaderBeforeSize = 0; }
        if (preloaderAfterSize === void 0) { preloaderAfterSize = PRELOADER_WINDOW_SIZE; }
        this._unloadedElementIndices = [];
        this._currentIndex = -1;
        this._elementLoaders = elementLoaders;
        this._preloaderBeforeSize = preloaderBeforeSize;
        this._preloaderAfterSize = preloaderAfterSize;
        this._updateUnloadedElementIndices();
    }
    LazyLoader.prototype.loadAll = function () {
        this._loadElements(0, this._elementLoaders.length);
    };
    LazyLoader.prototype.setCurrentIndex = function (newIndex) {
        if (this._currentIndex != newIndex) {
            this._currentIndex = newIndex;
            this._moveWindow();
        }
    };
    /**
     * Will directly load an Element without changing the window of loaded Elements. This
     * has to be handled separately by setCurrentIndex
     *
     * @return {Promise} Promise that will resolve with true once the Element is loaded
     *         or will resolve right away with false, if the Element is in loading state;
     *         It will reject an invalid index with a error message
     */
    LazyLoader.prototype.loadElement = function (index) {
        return __awaiter(this, void 0, void 0, function () {
            var loaded, location_1, wasLoaded_1;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!(index >= 0 && index < this._elementLoaders.length)) return [3 /*break*/, 6];
                        loaded = false;
                        if (!!this._unloadedElementIndices.includes(index)) return [3 /*break*/, 1];
                        loaded = true;
                        return [3 /*break*/, 5];
                    case 1:
                        console.log("LazyLoader#loadElement - index: ", index);
                        _a.label = 2;
                    case 2:
                        _a.trys.push([2, 4, , 5]);
                        return [4 /*yield*/, this._elementLoaders[index].load()];
                    case 3:
                        loaded = _a.sent();
                        if (loaded) {
                            location_1 = this._unloadedElementIndices.indexOf(index);
                            if (location_1 > -1) {
                                this._unloadedElementIndices.splice(location_1, 1);
                            }
                        }
                        return [3 /*break*/, 5];
                    case 4:
                        wasLoaded_1 = _a.sent();
                        loaded = wasLoaded_1;
                        return [3 /*break*/, 5];
                    case 5: return [2 /*return*/, loaded];
                    case 6: throw new Error("Preloader#loadElement -> invalid Index " + index);
                }
            });
        });
    };
    LazyLoader.prototype.getElementLoaderInfos = function () {
        return this._elementLoaders;
    };
    LazyLoader.prototype.getUnloadedElementIndices = function () {
        return this._unloadedElementIndices;
    };
    LazyLoader.prototype._moveWindow = function () {
        if (this._unloadedElementIndices.length) {
            var start = this._currentIndex - this._preloaderBeforeSize;
            var end = this._currentIndex + this._preloaderAfterSize;
            // 1) load from current Element forward
            this._loadElements(this._currentIndex, end < this._elementLoaders.length ? end : this._elementLoaders.length);
            // 2) load from back towards current Element
            this._loadElements(start >= 0 ? start : 0, this._currentIndex);
            // 3) handle overflow
            if (start < 0) {
                start = this._elementLoaders.length + start;
                this._loadElements(start, this._elementLoaders.length);
            }
            if (end >= this._elementLoaders.length) {
                end = end - this._elementLoaders.length;
                this._loadElements(0, end);
            }
        }
    };
    LazyLoader.prototype._loadElements = function (start, end) {
        for (var i = start < 0 ? 0 : start; i < end && i < this._elementLoaders.length; i++) {
            this.loadElement(i);
        }
    };
    LazyLoader.prototype._updateUnloadedElementIndices = function () {
        for (var i = 0; i < this._elementLoaders.length; i++) {
            if (!this._elementLoaders[i].wasLoaded()) {
                this._unloadedElementIndices.push(i);
            }
        }
    };
    return LazyLoader;
}());
exports.default = LazyLoader;


/***/ }),

/***/ 325:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_37080__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
var mibreit_dom_tools_1 = __nested_webpack_require_37080__(942);
var debounce_1 = __nested_webpack_require_37080__(409);
// avoid overloading of scroll event
var SCROLL_EVENT_TIMEOUT = 400;
var ScrollLoader = /** @class */ (function () {
    function ScrollLoader(lazyLoader, elementLocations) {
        this._elementLocations = [];
        this._lazyLoader = lazyLoader;
        this._elementLocations = elementLocations;
    }
    ScrollLoader.prototype.startLoader = function () {
        var _this = this;
        this._loadElementsWithinWindowRect();
        var debouncedLoadEvent = debounce_1.default(function () { _this._loadElementsWithinWindowRect(); }, SCROLL_EVENT_TIMEOUT);
        mibreit_dom_tools_1.DomTools.addScrollEventListener(function (_event) {
            debouncedLoadEvent();
        });
    };
    ScrollLoader.prototype._loadElementsWithinWindowRect = function () {
        var unloadedElementIndices = this._lazyLoader.getUnloadedElementIndices();
        console.log('ScrollLoader#loadElementsWithinWindowRect - unloadedElements: ', unloadedElementIndices);
        for (var i = 0; i < unloadedElementIndices.length; i++) {
            if (this._elementLocations[unloadedElementIndices[i]].isElementWithinScrollArea()) {
                this._lazyLoader.loadElement(unloadedElementIndices[i]);
            }
        }
        // TODO: Further optimization -> stop once we leave the window area
    };
    return ScrollLoader;
}());
exports.default = ScrollLoader;


/***/ }),

/***/ 130:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_38868__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.Element = exports.createLazyLoader = exports.ELazyMode = void 0;
var createLazyLoader_1 = __nested_webpack_require_38868__(194);
Object.defineProperty(exports, "ELazyMode", ({ enumerable: true, get: function () { return createLazyLoader_1.ELazyMode; } }));
var createLazyLoader_2 = __nested_webpack_require_38868__(194);
Object.defineProperty(exports, "createLazyLoader", ({ enumerable: true, get: function () { return createLazyLoader_2.createLazyLoaderFromElements; } }));
var Element_1 = __nested_webpack_require_38868__(371);
Object.defineProperty(exports, "Element", ({ enumerable: true, get: function () { return Element_1.default; } }));


/***/ }),

/***/ 194:
/***/ ((__unused_webpack_module, exports, __nested_webpack_require_39779__) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
exports.createLazyLoaderFromElements = exports.ELazyMode = void 0;
var LazyLoader_1 = __nested_webpack_require_39779__(304);
var Element_1 = __nested_webpack_require_39779__(371);
var ElementSurrogate_1 = __nested_webpack_require_39779__(924);
var ScrollLoader_1 = __nested_webpack_require_39779__(325);
var mibreit_dom_tools_1 = __nested_webpack_require_39779__(942);
var ELazyMode;
(function (ELazyMode) {
    ELazyMode[ELazyMode["SIMPLE_DEFER"] = 0] = "SIMPLE_DEFER";
    ELazyMode[ELazyMode["WINDOWED_EXTERNAL"] = 1] = "WINDOWED_EXTERNAL";
    ELazyMode[ELazyMode["WINDOWED_SCROLL"] = 2] = "WINDOWED_SCROLL";
})(ELazyMode = exports.ELazyMode || (exports.ELazyMode = {}));
function createLazyLoaderFromElements(elements, config) {
    if (config.useSurrogate) {
        var _loop_1 = function (i) {
            var surrogate = new ElementSurrogate_1.default(elements[i]);
            surrogate.wrap(elements[i].getHtmlElement());
            elements[i].addWasLoadedCallback(function () {
                surrogate.unwrap();
            });
        };
        for (var i = 0; i < elements.length; i++) {
            _loop_1(i);
        }
    }
    var lazyLoader = new LazyLoader_1.default(elements, config.preloaderBeforeSize, config.preloaderAfterSize);
    startLoader(lazyLoader, elements, config.mode);
    return lazyLoader;
}
exports.createLazyLoaderFromElements = createLazyLoaderFromElements;
function default_1(elementSelector, config) {
    var htmlElements = mibreit_dom_tools_1.DomTools.getElements(elementSelector);
    var elements = [];
    for (var i = 0; i < htmlElements.length; i++) {
        var element = new Element_1.default(htmlElements[i]);
        elements.push(element);
    }
    return createLazyLoaderFromElements(elements, config);
}
exports.default = default_1;
function startLoader(loader, elements, mode) {
    if (mode === void 0) { mode = ELazyMode.SIMPLE_DEFER; }
    if (mode != null) {
        switch (mode) {
            case ELazyMode.WINDOWED_EXTERNAL:
                loader.loadElement(0);
                loader.setCurrentIndex(0);
                break;
            case ELazyMode.WINDOWED_SCROLL:
                var scrollLoader = new ScrollLoader_1.default(loader, elements);
                scrollLoader.startLoader();
                break;
            case ELazyMode.SIMPLE_DEFER:
            default:
                loader.loadAll();
                break;
        }
    }
}


/***/ }),

/***/ 409:
/***/ ((__unused_webpack_module, exports) => {

"use strict";

/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
Object.defineProperty(exports, "__esModule", ({ value: true }));
function default_1(callback, wait) {
    var timeout;
    return function debouncedCallback() {
        var later = function () {
            clearTimeout(timeout);
            callback();
        };
        clearTimeout(timeout);
        timeout = window.setTimeout(later, wait);
    };
}
exports.default = default_1;
;


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __nested_webpack_require_43269__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		if(__webpack_module_cache__[moduleId]) {
/******/ 			return __webpack_module_cache__[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __nested_webpack_require_43269__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__nested_webpack_require_43269__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => module['default'] :
/******/ 				() => module;
/******/ 			__nested_webpack_require_43269__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__nested_webpack_require_43269__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__nested_webpack_require_43269__.o(definition, key) && !__nested_webpack_require_43269__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__nested_webpack_require_43269__.o = (obj, prop) => Object.prototype.hasOwnProperty.call(obj, prop)
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__nested_webpack_require_43269__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	// module exports must be returned from runtime so entry inlining is disabled
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	return __nested_webpack_require_43269__(130);
/******/ })()

));

/***/ }),

/***/ 379:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ 355:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "DomTools": () => (/* reexport */ lib.DomTools),
  "EImageScaleMode": () => (/* reexport */ EImageScaleMode),
  "createGallery": () => (/* reexport */ createGallery),
  "createSlideshow": () => (/* reexport */ createSlideshow),
  "createThumbScrollerView": () => (/* reexport */ createThumbScrollerView)
});

// EXTERNAL MODULE: ./node_modules/mibreit-dom-tools/lib/index.js
var lib = __webpack_require__(942);
// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js
var injectStylesIntoStyleTag = __webpack_require__(379);
var injectStylesIntoStyleTag_default = /*#__PURE__*/__webpack_require__.n(injectStylesIntoStyleTag);
// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ImageStage.module.css
var ImageStage_module = __webpack_require__(710);
;// CONCATENATED MODULE: ./src/components/ImageStage.module.css

            

var options = {"injectType":"singletonStyleTag"};

options.insert = "head";
options.singleton = true;

var update = injectStylesIntoStyleTag_default()(ImageStage_module/* default */.Z, options);



/* harmony default export */ const components_ImageStage_module = (ImageStage_module/* default.locals */.Z.locals || {});
// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/tools/animations.module.css
var animations_module = __webpack_require__(614);
;// CONCATENATED MODULE: ./src/tools/animations.module.css

            

var animations_module_options = {"injectType":"singletonStyleTag"};

animations_module_options.insert = "head";
animations_module_options.singleton = true;

var animations_module_update = injectStylesIntoStyleTag_default()(animations_module/* default */.Z, animations_module_options);



/* harmony default export */ const tools_animations_module = (animations_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/components/ImageStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



/**
 * The ImageStage is responsible for proper scaling and centering
 * of images on the stage
 */
var ImageStage = /** @class */ (function () {
    function ImageStage(imageHandle, imageWidth, imageHeight) {
        this._zoomAnimation = false;
        this._imageHandle = imageHandle;
        this._imageWidth = imageWidth;
        this._imageHeight = imageHeight;
        this._imageStage = this._createStage();
    }
    ImageStage.prototype.setZoomAnimation = function (activate) {
        this._zoomAnimation = activate;
    };
    ImageStage.prototype.applyScaleMode = function () {
        console.log('ImageStage#applyScaleMode');
        var stageDimension = lib.DomTools.getElementDimension(this._imageStage);
        this._applyScaleModeImpl(stageDimension.width, stageDimension.height);
        this.centerImage(stageDimension.width, stageDimension.height);
    };
    ImageStage.prototype.setSize = function (widthCss, heightCss) {
        lib.DomTools.addCssStyle(this._imageStage, 'width', widthCss);
        lib.DomTools.addCssStyle(this._imageStage, 'height', heightCss);
        this.applyScaleMode();
    };
    ImageStage.prototype.setMargin = function (marginCss) {
        lib.DomTools.addCssStyle(this._imageStage, 'margin', marginCss);
    };
    ImageStage.prototype.hideImage = function () {
        var _this = this;
        // leave enough time for hide animation to be applied
        if (this._zoomAnimation) {
            setTimeout(function () {
                _this.resetZoom();
            }, 1000);
        }
        lib.DomTools.removeCssStyle(this._imageStage, 'opacity');
        lib.DomTools.removeCssStyle(this._imageStage, 'z-index');
    };
    ImageStage.prototype.showImage = function () {
        this.applyScaleMode();
        if (this._zoomAnimation) {
            this.startZoomAnimation();
        }
        lib.DomTools.addCssStyle(this._imageStage, 'opacity', '1');
        lib.DomTools.addCssStyle(this._imageStage, 'z-index', '1');
    };
    ImageStage.prototype._createStage = function () {
        var wrapper = lib.DomTools.createElement('div');
        lib.DomTools.addCssClass(wrapper, components_ImageStage_module.mibreit_ImageStage);
        lib.DomTools.addCssClass(wrapper, tools_animations_module.mibreit_GalleryFade);
        lib.DomTools.wrapElements([this._imageHandle], wrapper);
        return wrapper;
    };
    ImageStage.prototype.centerImage = function (stageWidth, stageHeight) {
        var _a = lib.DomTools.getElementDimension(this._imageHandle), width = _a.width, height = _a.height;
        var x = (width + stageWidth) / 2 - width;
        var y = (height + stageHeight) / 2 - height;
        lib.DomTools.addCssStyle(this._imageHandle, 'margin-left', x + "px");
        lib.DomTools.addCssStyle(this._imageHandle, 'margin-top', y + "px");
    };
    ImageStage.prototype.startZoomAnimation = function () {
        console.log('ImageStage#startZoomAnimation');
        lib.DomTools.addCssClass(this._imageHandle, 'zoom');
    };
    ImageStage.prototype.resetZoom = function () {
        console.log('ImageStage#resetZoom');
        lib.DomTools.removeCssClass(this._imageHandle, 'zoom');
    };
    return ImageStage;
}());
/* harmony default export */ const components_ImageStage = (ImageStage);

;// CONCATENATED MODULE: ./src/components/ImageStageFitAspect.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageFitAspect = /** @class */ (function (_super) {
    __extends(ImageStageFitAspect, _super);
    function ImageStageFitAspect(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageFitAspect.prototype._applyScaleModeImpl = function (stageWidth, stageHeight) {
        var aspect = this._imageWidth / this._imageHeight;
        var newWidth = 'auto';
        var newHeight = 'auto';
        if (stageWidth / stageHeight > aspect) {
            // fit based on height
            if (stageHeight <= this._imageHeight) {
                newHeight = "100%";
            }
        }
        else {
            // fit based on width
            if (stageWidth <= this._imageWidth) {
                newWidth = "100%";
            }
        }
        lib.DomTools.addCssStyle(this._imageHandle, 'width', newWidth);
        lib.DomTools.addCssStyle(this._imageHandle, 'height', newHeight);
    };
    return ImageStageFitAspect;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageFitAspect = (ImageStageFitAspect);

;// CONCATENATED MODULE: ./src/components/ImageStageExpand.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageExpand_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageExpand = /** @class */ (function (_super) {
    ImageStageExpand_extends(ImageStageExpand, _super);
    function ImageStageExpand(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageExpand.prototype._applyScaleModeImpl = function (stageWidth, stageHeight) {
        var aspect = this._imageWidth / this._imageHeight;
        if (stageWidth / stageHeight > aspect) {
            // fit based on width
            lib.DomTools.addCssStyle(this._imageHandle, 'width', "100%");
            lib.DomTools.addCssStyle(this._imageHandle, 'height', 100 / aspect + "%");
        }
        else {
            // fit based on height
            lib.DomTools.addCssStyle(this._imageHandle, 'width', aspect * 100 + "%");
            lib.DomTools.addCssStyle(this._imageHandle, 'height', "100%");
        }
    };
    return ImageStageExpand;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageExpand = (ImageStageExpand);

;// CONCATENATED MODULE: ./src/components/ImageStageStretch.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageStretch_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageStretch = /** @class */ (function (_super) {
    ImageStageStretch_extends(ImageStageStretch, _super);
    function ImageStageStretch(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageStretch.prototype._applyScaleModeImpl = function (_stageWidth, _stageHeight) {
        lib.DomTools.addCssStyle(this._imageHandle, 'width', "100%");
        lib.DomTools.addCssStyle(this._imageHandle, 'height', "100%");
    };
    return ImageStageStretch;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageStretch = (ImageStageStretch);

;// CONCATENATED MODULE: ./src/components/ImageStageNoScale.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageNoScale_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageNoScale = /** @class */ (function (_super) {
    ImageStageNoScale_extends(ImageStageNoScale, _super);
    function ImageStageNoScale(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageNoScale.prototype._applyScaleModeImpl = function (_stageWidth, _stageHeight) {
        lib.DomTools.removeCssStyle(this._imageHandle, 'width');
        lib.DomTools.removeCssStyle(this._imageHandle, 'height');
    };
    return ImageStageNoScale;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageNoScale = (ImageStageNoScale);

;// CONCATENATED MODULE: ./src/factories/createImageStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */




var EImageScaleMode;
(function (EImageScaleMode) {
    EImageScaleMode[EImageScaleMode["NONE"] = 0] = "NONE";
    EImageScaleMode[EImageScaleMode["FIT_ASPECT"] = 1] = "FIT_ASPECT";
    EImageScaleMode[EImageScaleMode["STRETCH"] = 2] = "STRETCH";
    EImageScaleMode[EImageScaleMode["EXPAND"] = 3] = "EXPAND";
})(EImageScaleMode || (EImageScaleMode = {}));
/* harmony default export */ function createImageStage(imageHandle, imageWidth, imageHeight, scaleMode) {
    if (scaleMode === void 0) { scaleMode = EImageScaleMode.FIT_ASPECT; }
    switch (scaleMode) {
        case EImageScaleMode.EXPAND:
            return new components_ImageStageExpand(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.FIT_ASPECT:
            return new components_ImageStageFitAspect(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.STRETCH:
            return new components_ImageStageStretch(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.NONE:
        default:
            return new components_ImageStageNoScale(imageHandle, imageWidth, imageHeight);
    }
}
;

// EXTERNAL MODULE: ./node_modules/mibreit-lazy-loader/lib/indexNpm.js
var indexNpm = __webpack_require__(199);
;// CONCATENATED MODULE: ./src/constants.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
// Css properties
var IMAGE_DATA_SRC_ATTRIBUTE = 'data-src';
var IMAGE_SRC_ATTRIBUTE = 'src';
var IMAGE_TITLE_ATTRIBUTE = 'title';
var IMAGE_DATA_TITLE_ATTRIBUTE = 'data-title';
// Css classes
var CSS_IMAGE_STATE_CLASS = 'mibreit-image-stage';
// Gallery Configuration
var PRELOADER_RIGHT_SIZE = 7;
var PRELOADER_LEFT_SIZE = 3;
var SLIDESHOW_INTERVAL = 5000;
var NUMBER_OF_VISIBLE_THUMBS = 7;
// Opacities
var GALLERY_BUTTONS_SHOW_OPACITY = 0.5;

;// CONCATENATED MODULE: ./src/components/Image.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var Image_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();



var Image = /** @class */ (function (_super) {
    Image_extends(Image, _super);
    function Image(imageHandle) {
        var _this = _super.call(this, imageHandle) || this;
        if (imageHandle.hasAttribute(IMAGE_TITLE_ATTRIBUTE)) {
            _this._removeTitle(imageHandle);
        }
        _this._title = imageHandle.getAttribute(IMAGE_DATA_TITLE_ATTRIBUTE);
        _this._limitMaxSizeTo(imageHandle, _this.getWidth(), _this.getHeight());
        lib.DomTools.disableContextMenu(imageHandle);
        lib.DomTools.disableDragging(imageHandle);
        return _this;
    }
    Image.prototype.getTitle = function () {
        return this._title;
    };
    Image.prototype.getUrl = function () {
        return this._element.hasAttribute('data-src')
            ? this._element.getAttribute('data-src')
            : this._element.getAttribute('src');
    };
    Image.prototype._limitMaxSizeTo = function (imageHandle, maxWidth, maxHeight) {
        lib.DomTools.overwriteCssStyles(imageHandle, "max-width: " + maxWidth + "px; max-height: " + maxHeight + "px");
    };
    Image.prototype._removeTitle = function (imageHandle) {
        imageHandle.setAttribute(IMAGE_DATA_TITLE_ATTRIBUTE, imageHandle.getAttribute(IMAGE_TITLE_ATTRIBUTE));
        imageHandle.removeAttribute(IMAGE_TITLE_ATTRIBUTE);
    };
    return Image;
}(indexNpm.Element));
/* harmony default export */ const components_Image = (Image);

;// CONCATENATED MODULE: ./src/components/ImageViewer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var ImageViewer = /** @class */ (function () {
    function ImageViewer(images, scaleMode) {
        var _this = this;
        if (scaleMode === void 0) { scaleMode = EImageScaleMode.FIT_ASPECT; }
        this._currentIndex = 0;
        this._imageStages = [];
        this._imageChangedCallbacks = new Array();
        this._images = images;
        this._prepareImageStages(images, scaleMode);
        if (this._isValidIndex(0)) {
            if (!this._images[0].wasLoaded()) {
                this._images[0].addWasLoadedCallback(function () {
                    _this._changeCurrentImage(0);
                });
            }
            else {
                this._changeCurrentImage(0);
            }
        }
    }
    ImageViewer.prototype.showImage = function (index) {
        var _this = this;
        if (this._isValidIndex(index)) {
            if (index != this._currentIndex) {
                if (!this._images[index].wasLoaded()) {
                    this._images[index].addWasLoadedCallback(function () {
                        _this._imageStages[_this._currentIndex].hideImage();
                        _this._changeCurrentImage(index);
                    });
                }
                else {
                    this._imageStages[this._currentIndex].hideImage();
                    this._changeCurrentImage(index);
                }
            }
            return true;
        }
        else {
            return false;
        }
    };
    ImageViewer.prototype.showNextImage = function () {
        var newIndex = this._currentIndex < this._imageStages.length - 1 ? this._currentIndex + 1 : 0;
        return this.showImage(newIndex);
    };
    ImageViewer.prototype.showPreviousImage = function () {
        var newIndex = this._currentIndex > 0 ? this._currentIndex - 1 : this._imageStages.length - 1;
        return this.showImage(newIndex);
    };
    ImageViewer.prototype.reinitSize = function () {
        this._imageStages[this._currentIndex].applyScaleMode();
    };
    ImageViewer.prototype.setZoomAnimation = function (active) {
        this._imageStages.forEach(function (stage) {
            stage.setZoomAnimation(active);
        });
    };
    ImageViewer.prototype.addImageChangedCallback = function (callback) {
        if (!this._imageChangedCallbacks.includes(callback)) {
            this._imageChangedCallbacks.push(callback);
        }
    };
    ImageViewer.prototype.getImageIndex = function () {
        return this._currentIndex;
    };
    ImageViewer.prototype.getImageInfo = function (index) {
        if (this._isValidIndex(index)) {
            return this._images[index];
        }
        else {
            return null;
        }
    };
    ImageViewer.prototype._prepareImageStages = function (images, scaleMode) {
        var _this = this;
        images.forEach(function (image) {
            var imageStage = createImageStage(image.getHtmlElement(), image.getWidth(), image.getHeight(), scaleMode);
            image.addWasLoadedCallback(function () {
                imageStage.applyScaleMode();
            });
            _this._imageStages.push(imageStage);
        });
    };
    ImageViewer.prototype._isValidIndex = function (index) {
        return index >= 0 && index < this._imageStages.length;
    };
    ImageViewer.prototype._changeCurrentImage = function (index) {
        var _this = this;
        console.log('ImageViewer#changeCurrentImage', index);
        this._currentIndex = index;
        this._imageStages[this._currentIndex].showImage();
        this._imageChangedCallbacks.forEach(function (callback) {
            callback(_this._currentIndex, _this._images[_this._currentIndex]);
        });
    };
    return ImageViewer;
}());
/* harmony default export */ const components_ImageViewer = (ImageViewer);

;// CONCATENATED MODULE: ./src/factories/createImageViewer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */


/* harmony default export */ function createImageViewer(images, scaleMode) {
    if (scaleMode === void 0) { scaleMode = EImageScaleMode.FIT_ASPECT; }
    var imageViewer = new components_ImageViewer(images, scaleMode);
    return imageViewer;
}

;// CONCATENATED MODULE: ./src/components/Slideshow.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */





var Slideshow = /** @class */ (function () {
    function Slideshow(config) {
        var _this = this;
        this._checkConfig(config);
        var images = this._prepareImages(lib.DomTools.getElements(config.imageSelector));
        this._loader = this._prepareLoader(images);
        this._imageViewer = this._prepareImageViewer(images, this._loader, config.scaleMode, config.zoom);
        if (config.interval !== undefined) {
            setInterval(function () {
                _this._imageViewer.showNextImage();
            }, config.interval);
        }
    }
    Slideshow.prototype.getViewer = function () {
        return this._imageViewer;
    };
    Slideshow.prototype.getLoader = function () {
        return this._loader;
    };
    Slideshow.prototype._checkConfig = function (config) {
        if (typeof config.imageSelector === 'undefined') {
            throw new Error('SlideshowConfig invalid: no imageSelector provided');
        }
    };
    Slideshow.prototype._prepareImages = function (imagesSelector) {
        var images = new Array();
        for (var i = 0; i < imagesSelector.length; i++) {
            var image = new components_Image(imagesSelector[i]);
            images.push(image);
        }
        return images;
    };
    Slideshow.prototype._prepareLoader = function (images) {
        var loader = (0,indexNpm.createLazyLoader)(images, {
            mode: indexNpm.ELazyMode.WINDOWED_EXTERNAL,
            preloaderBeforeSize: PRELOADER_LEFT_SIZE,
            preloaderAfterSize: PRELOADER_RIGHT_SIZE,
        });
        loader.setCurrentIndex(0);
        return loader;
    };
    Slideshow.prototype._prepareImageViewer = function (images, loader, scaleMode, zoom) {
        var imageViewer = createImageViewer(images, scaleMode);
        imageViewer.addImageChangedCallback(function (index, _imageInfo) {
            loader.setCurrentIndex(index);
        });
        if (zoom !== undefined) {
            imageViewer.setZoomAnimation(zoom);
        }
        return imageViewer;
    };
    return Slideshow;
}());
/* harmony default export */ const components_Slideshow = (Slideshow);

;// CONCATENATED MODULE: ./src/factories/createSlideshow.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

/* harmony default export */ function createSlideshow(config) {
    return new components_Slideshow(config);
}

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/HorizontalScroller.module.css
var HorizontalScroller_module = __webpack_require__(706);
;// CONCATENATED MODULE: ./src/components/HorizontalScroller.module.css

            

var HorizontalScroller_module_options = {"injectType":"singletonStyleTag"};

HorizontalScroller_module_options.insert = "head";
HorizontalScroller_module_options.singleton = true;

var HorizontalScroller_module_update = injectStylesIntoStyleTag_default()(HorizontalScroller_module/* default */.Z, HorizontalScroller_module_options);



/* harmony default export */ const components_HorizontalScroller_module = (HorizontalScroller_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/components/HorizontalScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */


var HorizontalScroller = /** @class */ (function () {
    function HorizontalScroller(container) {
        this._scroller = this._createScroller(container);
    }
    HorizontalScroller.prototype.scrollTo = function (position, useRem) {
        if (useRem === void 0) { useRem = false; }
        var positionCss;
        if (useRem) {
            positionCss = position + "rem";
        }
        else {
            positionCss = position + "px";
        }
        lib.DomTools.addCssStyle(this._scroller, 'transform', "translate(" + positionCss + ")");
        return true;
    };
    HorizontalScroller.prototype._createScroller = function (container) {
        var childNodes = lib.DomTools.getChildNodes(container);
        var scroller = lib.DomTools.createElement('div');
        lib.DomTools.addCssClass(scroller, components_HorizontalScroller_module.mibreit_HorizontalScroller);
        lib.DomTools.wrapElements(childNodes, scroller);
        return scroller;
    };
    return HorizontalScroller;
}());
/* harmony default export */ const components_HorizontalScroller = (HorizontalScroller);

;// CONCATENATED MODULE: ./src/components/ThumbScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var ThumbScroller = /** @class */ (function () {
    function ThumbScroller(layout) {
        this._scrollIndexChangedCallbacks = new Array();
        this._layout = layout;
        this._scroller = new components_HorizontalScroller(layout.getScrollerContainer());
        console.log('ThumbScroller#constructor - thumbSizeRem = ', this._layout.getThumbSizeRem(), ', numberOfThumbs = ', this._layout.getNumberOfThumbs(), ', numberOfVisibleThumbs = ', this._layout.getNumberOfVisibleThumbs());
    }
    ThumbScroller.prototype.reinitSize = function () {
        this._layout.reinitSize();
        this.scrollTo(this._currentScrollIndex, false);
    };
    ThumbScroller.prototype.scrollTo = function (index, useCenterIndex) {
        var _this = this;
        if (useCenterIndex === void 0) { useCenterIndex = true; }
        var newIndex = index;
        if (useCenterIndex) {
            newIndex -= Math.floor(this._layout.getNumberOfVisibleThumbs() / 2);
        }
        var normalizedIndex = this._normalizeIndex(newIndex);
        console.log('ThumbScroller#scrollTo', newIndex, normalizedIndex);
        this._currentScrollIndex = normalizedIndex;
        var currentScrollPosition = -normalizedIndex * this._layout.getThumbSizeRem();
        this._scroller.scrollTo(currentScrollPosition, true);
        this._scrollIndexChangedCallbacks.forEach(function (callback) {
            callback(_this._currentScrollIndex);
        });
    };
    ThumbScroller.prototype.scrollNext = function () {
        var newIndex = this._currentScrollIndex + this._layout.getNumberOfVisibleThumbs();
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (this._currentScrollIndex === maxPos) {
            newIndex = 0;
        }
        else if (newIndex >= maxPos) {
            newIndex = maxPos;
        }
        this.scrollTo(newIndex, false);
    };
    ThumbScroller.prototype.scrollPrevious = function () {
        var newIndex = this._currentScrollIndex - this._layout.getNumberOfVisibleThumbs();
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (this._currentScrollIndex === 0) {
            newIndex = maxPos;
        }
        else if (newIndex < 0) {
            newIndex = 0;
        }
        this.scrollTo(newIndex, false);
    };
    ThumbScroller.prototype.addScrollIndexChangedCallback = function (callback) {
        if (!this._scrollIndexChangedCallbacks.includes(callback)) {
            this._scrollIndexChangedCallbacks.push(callback);
        }
    };
    ThumbScroller.prototype._normalizeIndex = function (index) {
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (index >= maxPos) {
            return maxPos;
        }
        else if (index < 0) {
            return 0;
        }
        else {
            return index;
        }
    };
    return ThumbScroller;
}());
/* harmony default export */ const components_ThumbScroller = (ThumbScroller);

;// CONCATENATED MODULE: ./src/factories/createThumbScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

/* harmony default export */ function createThumbScroller(thumbScrollerLayout) {
    return new components_ThumbScroller(thumbScrollerLayout);
}

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ThumbStage.module.css
var ThumbStage_module = __webpack_require__(867);
;// CONCATENATED MODULE: ./src/components/ThumbStage.module.css

            

var ThumbStage_module_options = {"injectType":"singletonStyleTag"};

ThumbStage_module_options.insert = "head";
ThumbStage_module_options.singleton = true;

var ThumbStage_module_update = injectStylesIntoStyleTag_default()(ThumbStage_module/* default */.Z, ThumbStage_module_options);



/* harmony default export */ const components_ThumbStage_module = (ThumbStage_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/components/ThumbStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ThumbStage_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();



var ThumbStage = /** @class */ (function (_super) {
    ThumbStage_extends(ThumbStage, _super);
    function ThumbStage(imageHandle, imageWidth, imageHeight) {
        var _this = _super.call(this, imageHandle, imageWidth, imageHeight) || this;
        lib.DomTools.addCssClass(_this._imageStage, components_ThumbStage_module.mibreit_ThumbStage);
        return _this;
    }
    ThumbStage.prototype.addStageClickedCallback = function (callback) {
        lib.DomTools.addClickEventListener(this._imageStage, callback);
    };
    return ThumbStage;
}(components_ImageStageExpand));
/* harmony default export */ const components_ThumbStage = (ThumbStage);

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ThumbScrollerLayout.module.css
var ThumbScrollerLayout_module = __webpack_require__(136);
;// CONCATENATED MODULE: ./src/components/ThumbScrollerLayout.module.css

            

var ThumbScrollerLayout_module_options = {"injectType":"singletonStyleTag"};

ThumbScrollerLayout_module_options.insert = "head";
ThumbScrollerLayout_module_options.singleton = true;

var ThumbScrollerLayout_module_update = injectStylesIntoStyleTag_default()(ThumbScrollerLayout_module/* default */.Z, ThumbScrollerLayout_module_options);



/* harmony default export */ const components_ThumbScrollerLayout_module = (ThumbScrollerLayout_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/images/nextThumbs.svg
/* harmony default export */ const nextThumbs = ("<svg viewBox=\"0 0 1792 1792\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M1152 896q0 26-19 45l-448 448q-19 19-45 19t-45-19-19-45v-896q0-26 19-45t45-19 45 19l448 448q19 19 19 45z\"/></svg>");
;// CONCATENATED MODULE: ./src/components/ThumbScrollerLayout.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



var ThumbScrollerLayout = /** @class */ (function () {
    function ThumbScrollerLayout(container, thumbStages, numberOfVisibleThumbs) {
        this._numberOfVisibleThumbs = numberOfVisibleThumbs;
        this._thumbStages = thumbStages;
        var willThumbsFitContainer = numberOfVisibleThumbs >= thumbStages.length;
        this._createScrollerContainer(container, willThumbsFitContainer);
        this._createScrollerButtons(container, willThumbsFitContainer);
        lib.DomTools.addCssClass(container, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerParentContainer);
        this.reinitSize();
    }
    ThumbScrollerLayout.prototype.reinitSize = function () {
        this._thumbSizeRem = this._calculateThumbsize(this._scrollerContainer, this._numberOfVisibleThumbs);
        this._resizeThumbStages(this._thumbSizeRem);
    };
    ThumbScrollerLayout.prototype.getThumbSizeRem = function () {
        return this._thumbSizeRem;
    };
    ThumbScrollerLayout.prototype.getNumberOfVisibleThumbs = function () {
        return this._numberOfVisibleThumbs;
    };
    ThumbScrollerLayout.prototype.getNumberOfThumbs = function () {
        return this._thumbStages.length;
    };
    ThumbScrollerLayout.prototype.getThumbScrollerButtons = function () {
        return { previousButton: this._previousButton, nextButton: this._nextButton };
    };
    ThumbScrollerLayout.prototype.getScrollerContainer = function () {
        return this._scrollerContainer;
    };
    ThumbScrollerLayout.prototype._createScrollerContainer = function (container, centerThumbs) {
        var childNodes = lib.DomTools.getChildNodes(container);
        this._scrollerContainer = lib.DomTools.createElement('div');
        if (centerThumbs) {
            lib.DomTools.addCssClass(this._scrollerContainer, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerContainerCentered);
        }
        else {
            lib.DomTools.addCssClass(this._scrollerContainer, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerContainer);
        }
        lib.DomTools.wrapElements(childNodes, this._scrollerContainer);
    };
    ThumbScrollerLayout.prototype._calculateThumbsize = function (container, numberOfVisibleThumbs) {
        var oneRemSize = lib.DomTools.getRootFontSize();
        var containerWidthRem = lib.DomTools.getElementDimension(container).width / oneRemSize;
        var thumbsize = containerWidthRem / numberOfVisibleThumbs;
        return thumbsize;
    };
    ThumbScrollerLayout.prototype._createScrollerButtons = function (container, hidden) {
        this._previousButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(this._previousButton, nextThumbs);
        lib.DomTools.addCssClass(this._previousButton, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerPrevious);
        lib.DomTools.prependChildElement(this._previousButton, container);
        this._nextButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(this._nextButton, nextThumbs);
        lib.DomTools.addCssClass(this._nextButton, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerNext);
        lib.DomTools.appendChildElement(this._nextButton, container);
        if (hidden) {
            lib.DomTools.addCssStyle(this._previousButton, 'opacity', '0');
            lib.DomTools.addCssStyle(this._nextButton, 'opacity', '0');
        }
    };
    ThumbScrollerLayout.prototype._resizeThumbStages = function (size) {
        var innerSize = size * 0.9;
        var margin = size * 0.05;
        this._thumbStages.forEach(function (thumbStage) {
            thumbStage.setSize(innerSize + "rem", innerSize + "rem");
            thumbStage.setMargin(margin + "rem");
        });
    };
    return ThumbScrollerLayout;
}());
/* harmony default export */ const components_ThumbScrollerLayout = (ThumbScrollerLayout);

;// CONCATENATED MODULE: ./src/factories/createThumbScrollerLayout.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



function prepareThumbStages(thumbs, thumbClickedCallback) {
    var thumbStages = new Array();
    thumbs.forEach(function (thumb, index) {
        var thumbStage = new components_ThumbStage(thumb.getHtmlElement(), thumb.getWidth(), thumb.getHeight());
        thumb.addWasLoadedCallback(function () {
            thumbStage.applyScaleMode();
        });
        thumbStage.showImage();
        thumbStage.addStageClickedCallback(function () {
            thumbClickedCallback(index);
        });
        thumbStages.push(thumbStage);
    });
    return thumbStages;
}
/* harmony default export */ function createThumbScrollerLayout(container, thumbs, numberOfVisibleThumbs, thumbClickedCallback) {
    if (numberOfVisibleThumbs === void 0) { numberOfVisibleThumbs = NUMBER_OF_VISIBLE_THUMBS; }
    var thumbStages = prepareThumbStages(thumbs, thumbClickedCallback);
    return new components_ThumbScrollerLayout(container, thumbStages, numberOfVisibleThumbs);
}

;// CONCATENATED MODULE: ./src/components/ThumbScrollerView.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */





var ThumbScrollerView = /** @class */ (function () {
    function ThumbScrollerView(config, thumbClickedCallback) {
        this._thumbScroller = null;
        this._checkConfig(config);
        var thumbs = this._prepareThumbs(lib.DomTools.getElements(config.thumbSelector));
        this._loader = (0,indexNpm.createLazyLoader)(thumbs, {
            preloaderBeforeSize: config.numberOfVisibleThumbs,
            preloaderAfterSize: config.numberOfVisibleThumbs,
        });
        var layout = createThumbScrollerLayout(lib.DomTools.getElement(config.thumbContainerSelector), thumbs, config.numberOfVisibleThumbs, thumbClickedCallback);
        if (config.numberOfVisibleThumbs < thumbs.length) {
            this._thumbScroller = this._prepareThumbScroller(layout, this._loader);
            this._addThumbScrollerInteraction(this._thumbScroller, layout);
        }
    }
    ThumbScrollerView.prototype.getScroller = function () {
        return this._thumbScroller;
    };
    ThumbScrollerView.prototype._checkConfig = function (config) {
        if (typeof config.thumbContainerSelector === 'undefined') {
            throw new Error('ThumbScrollerConfig invalid: no thumbContainerSelector provided');
        }
        if (typeof config.thumbSelector === 'undefined') {
            throw new Error('ThumbScrollerConfig invalid: no thumbSelector provided');
        }
    };
    ThumbScrollerView.prototype._prepareThumbs = function (thumbSelector) {
        var thumbs = new Array();
        for (var i = 0; i < thumbSelector.length; i++) {
            var image = new components_Image(thumbSelector[i]);
            thumbs.push(image);
        }
        return thumbs;
    };
    ThumbScrollerView.prototype._prepareThumbScroller = function (layout, loader, initialIndex) {
        if (initialIndex === void 0) { initialIndex = 0; }
        var thumbScroller = createThumbScroller(layout);
        thumbScroller.addScrollIndexChangedCallback(function (index) {
            loader.setCurrentIndex(index);
        });
        thumbScroller.scrollTo(initialIndex);
        return thumbScroller;
    };
    ThumbScrollerView.prototype._addThumbScrollerInteraction = function (thumbScroller, thumbScrollerLayout) {
        var _a = thumbScrollerLayout.getThumbScrollerButtons(), previousButton = _a.previousButton, nextButton = _a.nextButton;
        lib.DomTools.addClickEventListener(nextButton, function () {
            thumbScroller.scrollNext();
        });
        lib.DomTools.addClickEventListener(previousButton, function () {
            thumbScroller.scrollPrevious();
        });
    };
    return ThumbScrollerView;
}());
/* harmony default export */ const components_ThumbScrollerView = (ThumbScrollerView);

;// CONCATENATED MODULE: ./src/factories/createThumbScrollerView.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

/* harmony default export */ function createThumbScrollerView(config) {
    return new components_ThumbScrollerView(config);
}

;// CONCATENATED MODULE: ./src/components/SwipeHandler.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var TRESHHOLD = 50;
var MAXTIME = 700;
var ESwipeDirection;
(function (ESwipeDirection) {
    ESwipeDirection[ESwipeDirection["NONE"] = 0] = "NONE";
    ESwipeDirection[ESwipeDirection["LEFT"] = 1] = "LEFT";
    ESwipeDirection[ESwipeDirection["RIGHT"] = 2] = "RIGHT";
})(ESwipeDirection || (ESwipeDirection = {}));
var SwipeHander = /** @class */ (function () {
    function SwipeHander(target, callback) {
        var _this = this;
        this._swipeActive = false;
        this._touchStart = function (event) {
            console.log('SwipeHander#touchStart');
            _this._swipeActive = true;
            _this._touchStartTime = Date.now();
            _this._touchStartPosition = _this._getTouchPosition(event);
        };
        this._touchEnd = function (event) {
            console.log('SwipeHander#touchEnd');
            if (_this._swipeActive && Date.now() - _this._touchStartTime < MAXTIME) {
                var touchEndPosition = _this._getTouchPosition(event);
                var direction = _this._detectDirection(_this._touchStartPosition, touchEndPosition);
                console.log('SwipeHander#touchEnd - direction: ', direction);
                _this._callback(direction, touchEndPosition);
            }
            _this._swipeActive = false;
        };
        this._callback = callback;
        lib.DomTools.addEventListener(target, 'pointerdown', this._touchStart);
        lib.DomTools.addEventListener(target, 'pointerup', this._touchEnd);
    }
    SwipeHander.prototype._getTouchPosition = function (event) {
        return {
            x: event.pageX,
            y: event.pageY,
        };
    };
    SwipeHander.prototype._detectDirection = function (start, end) {
        var diff = start.x - end.x;
        if (diff > TRESHHOLD) {
            return ESwipeDirection.RIGHT;
        }
        else if (diff < -TRESHHOLD) {
            return ESwipeDirection.LEFT;
        }
        else {
            return ESwipeDirection.NONE;
        }
    };
    return SwipeHander;
}());
/* harmony default export */ const SwipeHandler = (SwipeHander);

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/FullscreenView.module.css
var FullscreenView_module = __webpack_require__(986);
;// CONCATENATED MODULE: ./src/components/FullscreenView.module.css

            

var FullscreenView_module_options = {"injectType":"singletonStyleTag"};

FullscreenView_module_options.insert = "head";
FullscreenView_module_options.singleton = true;

var FullscreenView_module_update = injectStylesIntoStyleTag_default()(FullscreenView_module/* default */.Z, FullscreenView_module_options);



/* harmony default export */ const components_FullscreenView_module = (FullscreenView_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/images/close.svg
/* harmony default export */ const images_close = ("<svg viewBox=\"0 0 512 512\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\"><path d=\"M437.5,386.6L306.9,256l130.6-130.6c14.1-14.1,14.1-36.8,0-50.9c-14.1-14.1-36.8-14.1-50.9,0L256,205.1L125.4,74.5  c-14.1-14.1-36.8-14.1-50.9,0c-14.1,14.1-14.1,36.8,0,50.9L205.1,256L74.5,386.6c-14.1,14.1-14.1,36.8,0,50.9  c14.1,14.1,36.8,14.1,50.9,0L256,306.9l130.6,130.6c14.1,14.1,36.8,14.1,50.9,0C451.5,423.4,451.5,400.6,437.5,386.6z\"/></svg>");
;// CONCATENATED MODULE: ./src/components/FullscreenView.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



var GALLERY_PLACEHOLDER_ID = 'galleryContainerPlaceholder';
var THUMBS_PLACEHOLDER_ID = 'thumbContainerPlaceholder';
var FullscreenView = /** @class */ (function () {
    function FullscreenView(galleryContainer, thumbContainer) {
        this._fullscreenChangedCallbacks = [];
        this._fullScreenContainer = null;
        this._galleryContainerPlaceholder = null;
        this._thumbContainerPlaceholder = null;
        this._fullScreenCloseButton = null;
        this._fullscreenActive = false;
        this._galleryContainer = galleryContainer;
        this._thumbContainer = thumbContainer;
    }
    FullscreenView.prototype.activate = function () {
        if (!this._fullscreenActive) {
            if (this._fullScreenContainer == null) {
                this._createFullscreenContainer();
            }
            this._moveGalleryToFullscreen();
            if (this._thumbContainer) {
                this._moveThumbsToFullscreen();
            }
            this._addFullscreen();
            this._setupCloseButtonHandler();
            this._fullscreenActive = true;
            this._fullscreenChangedCallbacks.forEach(function (callback) {
                callback(true);
            });
        }
    };
    FullscreenView.prototype.deActivate = function () {
        if (this._fullscreenActive) {
            this._removeGalleryFromFullscreen();
            if (this._thumbContainer) {
                this._removeThumbsFromFullscreen();
            }
            this._removeFullscreen();
            this._fullscreenActive = false;
            this._fullscreenChangedCallbacks.forEach(function (callback) {
                callback(false);
            });
        }
    };
    FullscreenView.prototype.addFullscreenChangedCallback = function (callback) {
        if (!this._fullscreenChangedCallbacks.includes(callback)) {
            this._fullscreenChangedCallbacks.push(callback);
        }
    };
    FullscreenView.prototype.isFullscreenActive = function () {
        return this._fullscreenActive;
    };
    FullscreenView.prototype._createFullscreenContainer = function () {
        this._fullScreenContainer = lib.DomTools.createElement('div');
        lib.DomTools.addCssClass(this._fullScreenContainer, components_FullscreenView_module.mibreit_Fullscreen);
        this._galleryContainerPlaceholder = lib.DomTools.createElement('div');
        lib.DomTools.setAttribute(this._galleryContainerPlaceholder, 'id', GALLERY_PLACEHOLDER_ID);
        this._thumbContainerPlaceholder = lib.DomTools.createElement('div');
        lib.DomTools.setAttribute(this._thumbContainerPlaceholder, 'id', THUMBS_PLACEHOLDER_ID);
        this._fullScreenCloseButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(this._fullScreenCloseButton, images_close);
        lib.DomTools.addCssClass(this._fullScreenCloseButton, components_FullscreenView_module.mibreit_Fullscreen_exit);
        lib.DomTools.appendChildElement(this._fullScreenCloseButton, this._fullScreenContainer);
    };
    FullscreenView.prototype._addFullscreen = function () {
        var body = lib.DomTools.getElement('body');
        lib.DomTools.appendChildElement(this._fullScreenContainer, body);
    };
    FullscreenView.prototype._removeFullscreen = function () {
        lib.DomTools.removeElement(this._fullScreenContainer);
    };
    FullscreenView.prototype._moveGalleryToFullscreen = function () {
        lib.DomTools.prependBeforeChild(this._galleryContainerPlaceholder, this._galleryContainer);
        lib.DomTools.appendChildElement(this._galleryContainer, this._fullScreenContainer);
        lib.DomTools.addCssStyle(this._galleryContainer, 'width', '100%');
        lib.DomTools.addCssStyle(this._galleryContainer, 'flex-grow', '1');
    };
    FullscreenView.prototype._removeGalleryFromFullscreen = function () {
        lib.DomTools.removeElement(this._galleryContainer);
        lib.DomTools.prependBeforeChild(this._galleryContainer, this._galleryContainerPlaceholder);
        lib.DomTools.removeElement(this._galleryContainerPlaceholder);
        lib.DomTools.removeCssStyle(this._galleryContainer, 'width');
        lib.DomTools.removeCssStyle(this._galleryContainer, 'flex-grow');
    };
    FullscreenView.prototype._moveThumbsToFullscreen = function () {
        lib.DomTools.prependBeforeChild(this._thumbContainerPlaceholder, this._thumbContainer);
        lib.DomTools.appendChildElement(this._thumbContainer, this._fullScreenContainer);
        lib.DomTools.addCssStyle(this._thumbContainer, 'flex-grow', '0');
    };
    FullscreenView.prototype._removeThumbsFromFullscreen = function () {
        lib.DomTools.removeElement(this._thumbContainer);
        lib.DomTools.prependBeforeChild(this._thumbContainer, this._thumbContainerPlaceholder);
        lib.DomTools.removeElement(this._thumbContainerPlaceholder);
        lib.DomTools.removeCssStyle(this._thumbContainer, 'flex-grow');
    };
    FullscreenView.prototype._setupCloseButtonHandler = function () {
        var _this = this;
        lib.DomTools.addClickEventListener(this._fullScreenCloseButton, function (event) {
            event.stopPropagation();
            if (_this._fullscreenActive) {
                _this.deActivate();
            }
        });
    };
    return FullscreenView;
}());
/* harmony default export */ const components_FullscreenView = (FullscreenView);

;// CONCATENATED MODULE: ./src/factories/createFullscreen.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

/* harmony default export */ function createFullscreen(galleryContainer, thumbContainer) {
    return new components_FullscreenView(galleryContainer, thumbContainer);
}
;

;// CONCATENATED MODULE: ./src/tools/debounce.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
/* harmony default export */ function debounce(callback, wait, smooth) {
    if (smooth === void 0) { smooth = true; }
    var timeout;
    var waiting = false;
    if (smooth) {
        return function debouncedCallback() {
            if (!waiting) {
                var later = function () {
                    waiting = false;
                    clearTimeout(timeout);
                    callback();
                };
                waiting = true;
                timeout = window.setTimeout(later, wait);
            }
        };
    }
    else {
        return function debouncedCallback() {
            var later = function () {
                clearTimeout(timeout);
                callback();
            };
            clearTimeout(timeout);
            timeout = window.setTimeout(later, wait);
        };
    }
}

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/Gallery.module.css
var Gallery_module = __webpack_require__(861);
;// CONCATENATED MODULE: ./src/components/Gallery.module.css

            

var Gallery_module_options = {"injectType":"singletonStyleTag"};

Gallery_module_options.insert = "head";
Gallery_module_options.singleton = true;

var Gallery_module_update = injectStylesIntoStyleTag_default()(Gallery_module/* default */.Z, Gallery_module_options);



/* harmony default export */ const components_Gallery_module = (Gallery_module/* default.locals */.Z.locals || {});
;// CONCATENATED MODULE: ./src/images/nextImage.svg
/* harmony default export */ const nextImage = ("<svg viewBox=\"0 0 1792 1792\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M1363 877l-742 742q-19 19-45 19t-45-19l-166-166q-19-19-19-45t19-45l531-531-531-531q-19-19-19-45t19-45l166-166q19-19 45-19t45 19l742 742q19 19 19 45t-19 45z\"/></svg>");
;// CONCATENATED MODULE: ./src/images/fullscreen.svg
/* harmony default export */ const fullscreen = ("<svg viewBox=\"0 0 14 14\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\"><path d=\"M2,9 L0,9 L0,14 L5,14 L5,12 L2,12 L2,9 L2,9 Z M0,5 L2,5 L2,2 L5,2 L5,0 L0,0 L0,5 L0,5 Z M12,12 L9,12 L9,14 L14,14 L14,9 L12,9 L12,12 L12,12 Z M9,0 L9,2 L12,2 L12,5 L14,5 L14,0 L9,0 L9,0 Z\"/></svg>");
;// CONCATENATED MODULE: ./src/components/Gallery.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */











var DEBOUNCE_TIMER = 500;
var Gallery = /** @class */ (function () {
    function Gallery(config) {
        var _this = this;
        this._checkConfig(config);
        var container = lib.DomTools.getElement(config.galleryContainerSelector);
        this._slideShow = new components_Slideshow(config);
        var thumbScroller = new components_ThumbScrollerView(config, function (index) {
            _this._slideShow.getLoader().setCurrentIndex(index);
            _this._slideShow.getViewer().showImage(index);
        }).getScroller();
        if (thumbScroller) {
            this._slideShow.getViewer().addImageChangedCallback(function (index, _imageInfo) {
                thumbScroller.scrollTo(index, true);
            });
        }
        var fullScreenView = createFullscreen(container, lib.DomTools.getElement(config.thumbContainerSelector));
        var _a = this._createPreviousNextButtons(container), previousButton = _a.previousButton, nextButton = _a.nextButton;
        var fullscreenButton = this._createFullscreenButton(container);
        this._setupHoverEvents(container, [previousButton, nextButton, fullscreenButton]);
        this._setupKeyEvents(this.getViewer(), fullScreenView, fullscreenButton);
        this._setupSwipeHandler(container, this.getViewer());
        this._setupFullscreenClickEvent(fullscreenButton, fullScreenView, this.getViewer());
        this._setupResizeHandler(this.getViewer(), thumbScroller);
        this._setupFullscreenChangedHandler(fullScreenView, fullscreenButton, this.getViewer(), thumbScroller);
    }
    Gallery.prototype.getViewer = function () {
        return this._slideShow.getViewer();
    };
    Gallery.prototype.getLoader = function () {
        return this._slideShow.getLoader();
    };
    Gallery.prototype._checkConfig = function (config) {
        if (typeof config.galleryContainerSelector === 'undefined') {
            throw new Error('GalleryConfig invalid: no galleryContainerSelector provided');
        }
    };
    Gallery.prototype._createPreviousNextButtons = function (container) {
        var previousButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(previousButton, nextImage);
        lib.DomTools.addCssClass(previousButton, components_Gallery_module.mibreit_GalleryPrevious);
        lib.DomTools.addCssClass(previousButton, tools_animations_module.mibreit_GalleryFade);
        lib.DomTools.prependChildElement(previousButton, container);
        var nextButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(nextButton, nextImage);
        lib.DomTools.addCssClass(nextButton, components_Gallery_module.mibreit_GalleryNext);
        lib.DomTools.addCssClass(nextButton, tools_animations_module.mibreit_GalleryFade);
        lib.DomTools.appendChildElement(nextButton, container);
        return { previousButton: previousButton, nextButton: nextButton };
    };
    Gallery.prototype._createFullscreenButton = function (container) {
        var fullscreenButton = lib.DomTools.createElement('div');
        lib.DomTools.setInnerHtml(fullscreenButton, fullscreen);
        lib.DomTools.addCssClass(fullscreenButton, components_Gallery_module.mibreit_GalleryFullscreenButton);
        lib.DomTools.addCssClass(fullscreenButton, tools_animations_module.mibreit_GalleryFade);
        lib.DomTools.appendChildElement(fullscreenButton, container);
        return fullscreenButton;
    };
    Gallery.prototype._setupHoverEvents = function (container, buttons) {
        lib.DomTools.addEventListener(container, 'mouseenter', function () {
            buttons.forEach(function (button) {
                lib.DomTools.addCssStyle(button, 'opacity', "" + GALLERY_BUTTONS_SHOW_OPACITY);
            });
        });
        lib.DomTools.addEventListener(container, 'mouseleave', function () {
            buttons.forEach(function (button) {
                lib.DomTools.addCssStyle(button, 'opacity', '0');
            });
        });
    };
    Gallery.prototype._setupKeyEvents = function (imageViewer, fullScreen, fullscreenButton) {
        lib.DomTools.addKeyEventListener(function (event) {
            var key = lib.DomTools.getKeyFromEvent(event);
            switch (key) {
                case 'ArrowRight':
                    imageViewer.showNextImage();
                    break;
                case 'ArrowLeft':
                    imageViewer.showPreviousImage();
                    break;
                case 'Escape':
                    fullScreen.deActivate();
                    break;
                case 'f':
                    if (fullScreen.isFullscreenActive()) {
                        fullScreen.deActivate();
                    }
                    else {
                        fullScreen.activate();
                    }
                    break;
                default:
                    if (fullScreen.isFullscreenActive()) {
                        fullScreen.deActivate();
                    }
                    break;
            }
        });
    };
    Gallery.prototype._setupResizeHandler = function (imageViewer, thumbScroller) {
        if (thumbScroller) {
            var debouncedThumbResizer_1 = debounce(function () {
                thumbScroller.reinitSize();
            }, DEBOUNCE_TIMER, false);
            lib.DomTools.addResizeEventListener(function () {
                imageViewer.reinitSize();
                debouncedThumbResizer_1();
            });
        }
        else {
            lib.DomTools.addResizeEventListener(function () {
                imageViewer.reinitSize();
            });
        }
    };
    Gallery.prototype._setupSwipeHandler = function (container, imageViewer) {
        var containerWidth = lib.DomTools.getElementDimension(container).width;
        var containerPosX = lib.DomTools.getElementPosition(container).x;
        lib.DomTools.addCssStyle(container, 'touch-action', 'pinch-zoom pan-y');
        new SwipeHandler(container, function (direction, position) {
            if (direction === ESwipeDirection.LEFT) {
                imageViewer.showPreviousImage();
            }
            else if (direction === ESwipeDirection.RIGHT) {
                imageViewer.showNextImage();
            }
            else {
                if (position.x - containerPosX > containerWidth / 2) {
                    imageViewer.showNextImage();
                }
                else {
                    imageViewer.showPreviousImage();
                }
            }
        });
    };
    Gallery.prototype._setupFullscreenClickEvent = function (fullscreenButton, fullScreen, imageViewer) {
        lib.DomTools.addClickEventListener(fullscreenButton, function (event) {
            event.stopPropagation();
            if (!fullScreen.isFullscreenActive()) {
                fullScreen.activate();
                imageViewer.reinitSize();
                lib.DomTools.addCssStyle(fullscreenButton, 'display', 'none');
            }
        });
    };
    Gallery.prototype._setupFullscreenChangedHandler = function (fullscreenView, fullscreenButton, imageViewer, thumbScroller) {
        fullscreenView.addFullscreenChangedCallback(function (active) {
            if (thumbScroller) {
                thumbScroller.reinitSize();
            }
            imageViewer.reinitSize();
            if (active) {
                lib.DomTools.addCssStyle(fullscreenButton, 'display', 'none');
            }
            else {
                lib.DomTools.removeCssStyle(fullscreenButton, 'display');
            }
        });
    };
    return Gallery;
}());
/* harmony default export */ const components_Gallery = (Gallery);

;// CONCATENATED MODULE: ./src/factories/createGallery.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

/* harmony default export */ function createGallery(config) {
    return new components_Gallery(config);
}

;// CONCATENATED MODULE: ./src/index.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */







/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		if(__webpack_module_cache__[moduleId]) {
/******/ 			return __webpack_module_cache__[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	// module exports must be returned from runtime so entry inlining is disabled
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(355);
/******/ })()

));