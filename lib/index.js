/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ 393:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_GalleryFullscreenButton {
  position: absolute;
  z-index: 2;
  width: 2rem;
  height: 2rem;
  right: 0.25rem;
  top: 0.25rem;
  opacity: 0;
}

.mibreit_GalleryPrevious {
  position: absolute;
  z-index: 2;
  top: 50%;
  opacity: 0;
}

.mibreit_GalleryNext {
  position: absolute;
  z-index: 2;
  top: 50%;
  right: 0rem;
  opacity: 0;
}

.mibreit_GalleryNext > svg {
  width: 3rem;
  height: 3rem;
  margin-top: -1.5rem;
  fill: #727f88;
}

.mibreit_GalleryPrevious > svg {
  transform: scaleX(-1);
  width: 3rem;
  height: 3rem;
  margin-top: -1.5rem;  
  fill: #727f88;
}
`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_GalleryFullscreenButton": `mibreit_GalleryFullscreenButton`,
	"mibreit_GalleryPrevious": `mibreit_GalleryPrevious`,
	"mibreit_GalleryNext": `mibreit_GalleryNext`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 706:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_HorizontalScroller {
  height: 100%;
  display: flex;
  flex-direction: row;
  align-items: center;
  -webkit-transition: transform 600ms ease-in-out;
  -moz-transition: transform 600ms ease-in-out;
  -o-transition: transform 600ms ease-in-out;
  transition: transform 600ms ease-in-out;
}
`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_HorizontalScroller": `mibreit_HorizontalScroller`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 710:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_ImageStage {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0; 
  overflow: hidden;
}

.mibreit_ImageStage > img.zoom {
  -webkit-transition: transform 10000ms linear;
  -moz-transition: transform 10000ms linear;
  -o-transition: transform 10000ms linear;
  transition: transform 10000ms linear;
  -webkit-transform: scale(1.125);
  -moz-transform: scale(1.125);
  -o-transform: scale(1.125);
  transform: scale(1.125);
}
`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ImageStage": `mibreit_ImageStage`,
	"zoom": `zoom`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 136:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_ThumbScrollerParentContainer {
  display: flex;
  flex-direction: row;
  align-items: center;
}

.mibreit_ThumbScrollerContainer {
  height: 100%;
  width: 100%;
  overflow: hidden;
}

.mibreit_ThumbScrollerContainerCentered {
  height: 100%;
  width: 100%;
  overflow: hidden;
  display: flex;
  justify-content: center;
  align-items: center;
}

.mibreit_ThumbScrollerParentContainer svg {
  width: 3rem;
  height: 3rem;
  fill: #727f88;
}

.mibreit_ThumbScrollerPrevious,
.mibreit_ThumbScrollerNext {
  margin-right: 0.5rem;
  margin-left: 0.5rem;
}

.mibreit_ThumbScrollerPrevious > svg {
  transform: scaleX(-1);
}
`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ThumbScrollerParentContainer": `mibreit_ThumbScrollerParentContainer`,
	"mibreit_ThumbScrollerContainer": `mibreit_ThumbScrollerContainer`,
	"mibreit_ThumbScrollerContainerCentered": `mibreit_ThumbScrollerContainerCentered`,
	"mibreit_ThumbScrollerPrevious": `mibreit_ThumbScrollerPrevious`,
	"mibreit_ThumbScrollerNext": `mibreit_ThumbScrollerNext`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 867:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_ThumbStage {
  margin: 0.25rem;
  position: relative;
  flex-shrink: 0;
  -webkit-box-shadow: 0px 0px 5px 0px rgba(0,0,0,0.5); 
  box-shadow: 0px 0px 5px 0px rgba(0,0,0,0.5);
}

.mibreit_ThumbStage:hover {
  transform: scale(1.1);
}
`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_ThumbStage": `mibreit_ThumbStage`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 799:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_Fullscreen {
  position: fixed;
  box-sizing: border-box;
  background-color: #181818;
  -webkit-transition: background-color 750ms ease-in-out;
  -moz-transition: background-color 750ms ease-in-out;
  -o-transition: background-color 750ms ease-in-out;
  transition: background-color 750ms ease-in-out;
  overflow: hidden;
  top: 0;
  left: 0;
  padding: 1rem;
  width: 100%;
  height: 100%;
  z-index: 100;
  display: flex;
  flex-direction: column;
}

.mibreit_Fullscreen > div > .mibreit_ImageStage {
  overflow: visible;
}

.mibreit_Fullscreen > div > .mibreit_ImageStage img {
  -webkit-box-shadow: 0px 0px 15px 0px rgba(0,0,0,0.5); 
  box-shadow: 0px 0px 15px 0px rgba(0,0,0,0.5);
}

.mibreit_Fullscreen > .mibreit_Fullscreen_exit {
  position: absolute;
  width: 3rem;
  height: 3rem;
  right: 1rem;
  top: 1rem;
  z-index: 3;
}

.mibreit_Fullscreen > .mibreit_Fullscreen_exit > svg {
  opacity: 0.75;
}`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_Fullscreen": `mibreit_Fullscreen`,
	"mibreit_ImageStage": `mibreit_ImageStage`,
	"mibreit_Fullscreen_exit": `mibreit_Fullscreen_exit`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 614:
/***/ ((module, __webpack_exports__, __webpack_require__) => {

/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   Z: () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(81);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
// Imports


var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
// Module
___CSS_LOADER_EXPORT___.push([module.id, `.mibreit_GalleryFade
{
  -webkit-transition: opacity 750ms ease-out;
  -moz-transition: opacity 750ms ease-out;
  -o-transition: opacity 750ms ease-out;
  transition: opacity 750ms ease-out;
}
.mibreit_GalleryTransition
{
  -webkit-transition: all 750ms ease-out;
  -moz-transition: all 750ms ease-out;
  -o-transition: all 750ms ease-out;
  transition: all 750ms ease-out;
}`, ""]);
// Exports
___CSS_LOADER_EXPORT___.locals = {
	"mibreit_GalleryFade": `mibreit_GalleryFade`,
	"mibreit_GalleryTransition": `mibreit_GalleryTransition`
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 645:
/***/ ((module) => {



/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
module.exports = function (cssWithMappingToString) {
  var list = [];

  // return the list of modules as css string
  list.toString = function toString() {
    return this.map(function (item) {
      var content = "";
      var needLayer = typeof item[5] !== "undefined";
      if (item[4]) {
        content += "@supports (".concat(item[4], ") {");
      }
      if (item[2]) {
        content += "@media ".concat(item[2], " {");
      }
      if (needLayer) {
        content += "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {");
      }
      content += cssWithMappingToString(item);
      if (needLayer) {
        content += "}";
      }
      if (item[2]) {
        content += "}";
      }
      if (item[4]) {
        content += "}";
      }
      return content;
    }).join("");
  };

  // import a list of modules into the list
  list.i = function i(modules, media, dedupe, supports, layer) {
    if (typeof modules === "string") {
      modules = [[null, modules, undefined]];
    }
    var alreadyImportedModules = {};
    if (dedupe) {
      for (var k = 0; k < this.length; k++) {
        var id = this[k][0];
        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }
    for (var _k = 0; _k < modules.length; _k++) {
      var item = [].concat(modules[_k]);
      if (dedupe && alreadyImportedModules[item[0]]) {
        continue;
      }
      if (typeof layer !== "undefined") {
        if (typeof item[5] === "undefined") {
          item[5] = layer;
        } else {
          item[1] = "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {").concat(item[1], "}");
          item[5] = layer;
        }
      }
      if (media) {
        if (!item[2]) {
          item[2] = media;
        } else {
          item[1] = "@media ".concat(item[2], " {").concat(item[1], "}");
          item[2] = media;
        }
      }
      if (supports) {
        if (!item[4]) {
          item[4] = "".concat(supports);
        } else {
          item[1] = "@supports (".concat(item[4], ") {").concat(item[1], "}");
          item[4] = supports;
        }
      }
      list.push(item);
    }
  };
  return list;
};

/***/ }),

/***/ 81:
/***/ ((module) => {



module.exports = function (i) {
  return i[1];
};

/***/ }),

/***/ 379:
/***/ ((module) => {



var stylesInDOM = [];
function getIndexByIdentifier(identifier) {
  var result = -1;
  for (var i = 0; i < stylesInDOM.length; i++) {
    if (stylesInDOM[i].identifier === identifier) {
      result = i;
      break;
    }
  }
  return result;
}
function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];
  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var indexByIdentifier = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3],
      supports: item[4],
      layer: item[5]
    };
    if (indexByIdentifier !== -1) {
      stylesInDOM[indexByIdentifier].references++;
      stylesInDOM[indexByIdentifier].updater(obj);
    } else {
      var updater = addElementStyle(obj, options);
      options.byIndex = i;
      stylesInDOM.splice(i, 0, {
        identifier: identifier,
        updater: updater,
        references: 1
      });
    }
    identifiers.push(identifier);
  }
  return identifiers;
}
function addElementStyle(obj, options) {
  var api = options.domAPI(options);
  api.update(obj);
  var updater = function updater(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap && newObj.supports === obj.supports && newObj.layer === obj.layer) {
        return;
      }
      api.update(obj = newObj);
    } else {
      api.remove();
    }
  };
  return updater;
}
module.exports = function (list, options) {
  options = options || {};
  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];
    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDOM[index].references--;
    }
    var newLastIdentifiers = modulesToDom(newList, options);
    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];
      var _index = getIndexByIdentifier(_identifier);
      if (stylesInDOM[_index].references === 0) {
        stylesInDOM[_index].updater();
        stylesInDOM.splice(_index, 1);
      }
    }
    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ 569:
/***/ ((module) => {



var memo = {};

/* istanbul ignore next  */
function getTarget(target) {
  if (typeof memo[target] === "undefined") {
    var styleTarget = document.querySelector(target);

    // Special case to return head of iframe instead of iframe itself
    if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
      try {
        // This will throw an exception if access to iframe is blocked
        // due to cross-origin restrictions
        styleTarget = styleTarget.contentDocument.head;
      } catch (e) {
        // istanbul ignore next
        styleTarget = null;
      }
    }
    memo[target] = styleTarget;
  }
  return memo[target];
}

/* istanbul ignore next  */
function insertBySelector(insert, style) {
  var target = getTarget(insert);
  if (!target) {
    throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
  }
  target.appendChild(style);
}
module.exports = insertBySelector;

/***/ }),

/***/ 216:
/***/ ((module) => {



/* istanbul ignore next  */
function insertStyleElement(options) {
  var element = document.createElement("style");
  options.setAttributes(element, options.attributes);
  options.insert(element, options.options);
  return element;
}
module.exports = insertStyleElement;

/***/ }),

/***/ 565:
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {



/* istanbul ignore next  */
function setAttributesWithoutAttributes(styleElement) {
  var nonce =  true ? __webpack_require__.nc : 0;
  if (nonce) {
    styleElement.setAttribute("nonce", nonce);
  }
}
module.exports = setAttributesWithoutAttributes;

/***/ }),

/***/ 37:
/***/ ((module) => {



/* istanbul ignore next  */
var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join("\n");
  };
}();

/* istanbul ignore next  */
function apply(styleElement, index, remove, obj) {
  var css;
  if (remove) {
    css = "";
  } else {
    css = "";
    if (obj.supports) {
      css += "@supports (".concat(obj.supports, ") {");
    }
    if (obj.media) {
      css += "@media ".concat(obj.media, " {");
    }
    var needLayer = typeof obj.layer !== "undefined";
    if (needLayer) {
      css += "@layer".concat(obj.layer.length > 0 ? " ".concat(obj.layer) : "", " {");
    }
    css += obj.css;
    if (needLayer) {
      css += "}";
    }
    if (obj.media) {
      css += "}";
    }
    if (obj.supports) {
      css += "}";
    }
  }

  // For old IE
  /* istanbul ignore if  */
  if (styleElement.styleSheet) {
    styleElement.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = styleElement.childNodes;
    if (childNodes[index]) {
      styleElement.removeChild(childNodes[index]);
    }
    if (childNodes.length) {
      styleElement.insertBefore(cssNode, childNodes[index]);
    } else {
      styleElement.appendChild(cssNode);
    }
  }
}
var singletonData = {
  singleton: null,
  singletonCounter: 0
};

/* istanbul ignore next  */
function domAPI(options) {
  if (typeof document === "undefined") return {
    update: function update() {},
    remove: function remove() {}
  };

  // eslint-disable-next-line no-undef,no-use-before-define
  var styleIndex = singletonData.singletonCounter++;
  var styleElement =
  // eslint-disable-next-line no-undef,no-use-before-define
  singletonData.singleton || (
  // eslint-disable-next-line no-undef,no-use-before-define
  singletonData.singleton = options.insertStyleElement(options));
  return {
    update: function update(obj) {
      apply(styleElement, styleIndex, false, obj);
    },
    remove: function remove(obj) {
      apply(styleElement, styleIndex, true, obj);
    }
  };
}
module.exports = domAPI;

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/nonce */
/******/ 	(() => {
/******/ 		__webpack_require__.nc = undefined;
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other modules in the chunk.
(() => {
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  EImageScaleMode: () => (/* reexport */ EImageScaleMode),
  createFullscreenOnlyGallery: () => (/* reexport */ createFullscreenOnlyGallery),
  createGallery: () => (/* reexport */ createGallery),
  createSlideshow: () => (/* reexport */ createSlideshow),
  createThumbScrollerContainer: () => (/* reexport */ createThumbScroller)
});

;// CONCATENATED MODULE: ./node_modules/mibreit-dom-tools/lib/index.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
function documentReady(callback) {
    window.addEventListener('load', callback);
}
function getRootFontSize() {
    return parseFloat(window.getComputedStyle(document.body).getPropertyValue('font-size'));
}
function lib_createElement(tagName) {
    return document.createElement(tagName);
}
function createInputElement(type) {
    var input = document.createElement('input');
    input.type = type;
    return input;
}
function cloneElement(element) {
    return element.cloneNode();
}
function lib_removeElement(element) {
    element.remove();
}
function prependChildElement(element, parent) {
    parent.prepend(element);
}
function appendChildElement(element, parent) {
    parent.appendChild(element);
}
function prependBeforeChild(element, child) {
    child.parentElement.insertBefore(element, child);
}
function appendAfterChild(element, child) {
    child.parentElement.insertBefore(element, child.nextSibling);
}
function getChildNodes(element) {
    var nodes = element.childNodes;
    var nodesArray = new Array();
    for (var i = 0; i < nodes.length; ++i) {
        nodesArray.push(nodes[i]);
    }
    return nodesArray;
}
function setInnerHtml(parent, inner) {
    parent.innerHTML = inner;
}
function lib_wrapElements(elements, wrapper) {
    elements[0].parentNode.insertBefore(wrapper, elements[0]);
    elements.forEach(function (element) {
        wrapper.appendChild(element);
    });
}
function lib_unwrapElements(wrapper) {
    var elements = wrapper.childNodes;
    elements.forEach(function (element) {
        wrapper.parentNode.insertBefore(element, wrapper);
    });
}
function getParentElement(element) {
    return element.parentElement;
}
function lib_getElementDimension(element) {
    var elementRect = element.getBoundingClientRect();
    return {
        width: elementRect.width,
        height: elementRect.height,
    };
}
function lib_isElementWithinWindow(element) {
    var elementRect = element.getBoundingClientRect();
    var windowHeight = window.innerHeight;
    var windowWidth = window.innerWidth;
    return (elementRect.top + elementRect.height > 0 &&
        elementRect.top < windowHeight &&
        elementRect.left + elementRect.width > 0 &&
        elementRect.left < windowWidth);
}
function getElementPosition(element) {
    var rect = element.getBoundingClientRect();
    var scrollLeft = window.pageXOffset || document.documentElement.scrollLeft;
    var scrollTop = window.pageYOffset || document.documentElement.scrollTop;
    return { y: rect.top + scrollTop, x: rect.left + scrollLeft };
}
function getCssClasses(element) {
    return getAttribute(element, 'class');
}
function lib_addCssClass(element, cssClass) {
    if (!element.classList.contains(cssClass)) {
        element.classList.add(cssClass);
    }
}
function removeCssClass(element, cssClass) {
    if (element.classList.contains(cssClass)) {
        element.classList.remove(cssClass);
    }
}
function removeAllCssClasses(element) {
    if (element.hasAttribute('class')) {
        element.removeAttribute('class');
    }
}
function overwriteCssClasses(element, cssClasses) {
    if (cssClasses == null) {
        removeAllCssClasses(element);
    }
    else {
        element.setAttribute('class', cssClasses);
    }
}
function getCssStyle(element, styleName) {
    return element.style.getPropertyValue(styleName);
}
function getCssStyles(element) {
    return element.style.cssText;
}
function getComputedCssStyle(element, styleName) {
    return window.getComputedStyle(element).getPropertyValue(styleName);
}
function lib_addCssStyle(element, styleName, styleProperty) {
    element.style.setProperty(styleName, styleProperty);
}
function removeCssStyle(element, styleName) {
    element.style.removeProperty(styleName);
    if (element.style.length === 0) {
        element.removeAttribute('style');
    }
}
function removeAllCssStyles(element) {
    if (element.hasAttribute('style')) {
        element.removeAttribute('style');
    }
}
function lib_overwriteCssStyles(element, styles) {
    if (styles == null) {
        removeAllCssStyles(element);
    }
    else {
        element.style.cssText = styles;
    }
}
function hasAttribute(element, attribute) {
    return element.hasAttribute(attribute);
}
function getAttribute(element, attribute) {
    if (!element.hasAttribute(attribute)) {
        return null;
    }
    else {
        return element.getAttribute(attribute);
    }
}
function setAttribute(element, attribute, value) {
    element.setAttribute(attribute, value);
}
function removeAttribute(element, attribute) {
    element.removeAttribute(attribute);
}
function addClickEventListener(element, callback) {
    element.addEventListener('click', callback);
}
function addKeyEventListener(callback) {
    document.addEventListener('keydown', callback);
}
function lib_addScrollEventListener(callback) {
    document.addEventListener('scroll', callback);
}
function addResizeEventListener(callback) {
    window.addEventListener('resize', callback);
}
function addEventListener(element, event, callback) {
    element.addEventListener(event, callback);
}
function disableContextMenu(element) {
    element.addEventListener('contextmenu', function (e) {
        e.preventDefault();
    });
}
function disableDragging(element) {
    element.addEventListener('dragstart', function (e) {
        e.preventDefault();
    });
}
function getElement(selector) {
    return document.querySelector(selector);
}
function lib_getElements(selector) {
    return document.querySelectorAll(selector);
}
function getKeyFromEvent(event) {
    return event.key;
}
var DomTools = {
    documentReady: documentReady,
    getRootFontSize: getRootFontSize,
    createElement: lib_createElement,
    createInputElement: createInputElement,
    cloneElement: cloneElement,
    removeElement: lib_removeElement,
    prependChildElement: prependChildElement,
    appendChildElement: appendChildElement,
    prependBeforeChild: prependBeforeChild,
    appendAfterChild: appendAfterChild,
    getChildNodes: getChildNodes,
    setInnerHtml: setInnerHtml,
    wrapElements: lib_wrapElements,
    unwrapElements: lib_unwrapElements,
    getParentElement: getParentElement,
    getElementDimension: lib_getElementDimension,
    getElementPosition: getElementPosition,
    isElementWithinWindow: lib_isElementWithinWindow,
    getCssClasses: getCssClasses,
    addCssClass: lib_addCssClass,
    removeCssClass: removeCssClass,
    removeAllCssClasses: removeAllCssClasses,
    overwriteCssClasses: overwriteCssClasses,
    getCssStyle: getCssStyle,
    getCssStyles: getCssStyles,
    getComputedCssStyle: getComputedCssStyle,
    addCssStyle: lib_addCssStyle,
    removeCssStyle: removeCssStyle,
    removeAllCssStyles: removeAllCssStyles,
    overwriteCssStyles: lib_overwriteCssStyles,
    hasAttribute: hasAttribute,
    getAttribute: getAttribute,
    setAttribute: setAttribute,
    removeAttribute: removeAttribute,
    addClickEventListener: addClickEventListener,
    addKeyEventListener: addKeyEventListener,
    addScrollEventListener: lib_addScrollEventListener,
    addResizeEventListener: addResizeEventListener,
    addEventListener: addEventListener,
    disableContextMenu: disableContextMenu,
    disableDragging: disableDragging,
    getElement: getElement,
    getElements: lib_getElements,
    getKeyFromEvent: getKeyFromEvent,
};

// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/injectStylesIntoStyleTag.js
var injectStylesIntoStyleTag = __webpack_require__(379);
var injectStylesIntoStyleTag_default = /*#__PURE__*/__webpack_require__.n(injectStylesIntoStyleTag);
// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/singletonStyleDomAPI.js
var singletonStyleDomAPI = __webpack_require__(37);
var singletonStyleDomAPI_default = /*#__PURE__*/__webpack_require__.n(singletonStyleDomAPI);
// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/insertBySelector.js
var insertBySelector = __webpack_require__(569);
var insertBySelector_default = /*#__PURE__*/__webpack_require__.n(insertBySelector);
// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/setAttributesWithoutAttributes.js
var setAttributesWithoutAttributes = __webpack_require__(565);
var setAttributesWithoutAttributes_default = /*#__PURE__*/__webpack_require__.n(setAttributesWithoutAttributes);
// EXTERNAL MODULE: ./node_modules/style-loader/dist/runtime/insertStyleElement.js
var insertStyleElement = __webpack_require__(216);
var insertStyleElement_default = /*#__PURE__*/__webpack_require__.n(insertStyleElement);
// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ImageStage.module.css
var ImageStage_module = __webpack_require__(710);
;// CONCATENATED MODULE: ./src/components/ImageStage.module.css

      
      
      
      
      
      
      
      
      

var options = {};

;
options.setAttributes = (setAttributesWithoutAttributes_default());

      options.insert = insertBySelector_default().bind(null, "head");
    
options.domAPI = (singletonStyleDomAPI_default());
options.insertStyleElement = (insertStyleElement_default());

var update = injectStylesIntoStyleTag_default()(ImageStage_module/* default */.Z, options);




       /* harmony default export */ const components_ImageStage_module = (ImageStage_module/* default */.Z && ImageStage_module/* default */.Z.locals ? ImageStage_module/* default */.Z.locals : undefined);

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/tools/animations.module.css
var animations_module = __webpack_require__(614);
;// CONCATENATED MODULE: ./src/tools/animations.module.css

      
      
      
      
      
      
      
      
      

var animations_module_options = {};

;
animations_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      animations_module_options.insert = insertBySelector_default().bind(null, "head");
    
animations_module_options.domAPI = (singletonStyleDomAPI_default());
animations_module_options.insertStyleElement = (insertStyleElement_default());

var animations_module_update = injectStylesIntoStyleTag_default()(animations_module/* default */.Z, animations_module_options);




       /* harmony default export */ const tools_animations_module = (animations_module/* default */.Z && animations_module/* default */.Z.locals ? animations_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/components/SwipeHandler.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var TRESHHOLD = 50;
var MAXTIME = 700;
var ESwipeDirection;
(function (ESwipeDirection) {
    ESwipeDirection[ESwipeDirection["NONE"] = 0] = "NONE";
    ESwipeDirection[ESwipeDirection["LEFT"] = 1] = "LEFT";
    ESwipeDirection[ESwipeDirection["RIGHT"] = 2] = "RIGHT";
})(ESwipeDirection || (ESwipeDirection = {}));
var SwipeHander = /** @class */ (function () {
    function SwipeHander(target, callback) {
        var _this = this;
        this._touchStartTime = null;
        this._touchStartPosition = null;
        this._touchStart = function (event) {
            console.log('SwipeHander#touchStart');
            _this._touchStartTime = Date.now();
            _this._touchStartPosition = _this._getTouchPosition(event);
        };
        this._touchEnd = function (event) {
            console.log('SwipeHander#touchEnd');
            if (_this._touchStartTime && _this._touchStartPosition && Date.now() - _this._touchStartTime < MAXTIME) {
                var touchEndPosition = _this._getTouchPosition(event);
                var direction = _this._detectDirection(_this._touchStartPosition, touchEndPosition);
                console.log('SwipeHander#touchEnd - direction: ', direction);
                _this._callback(direction, touchEndPosition);
            }
            _this._touchStartPosition = null;
            _this._touchStartTime = null;
        };
        this._callback = callback;
        addEventListener(target, 'pointerdown', this._touchStart);
        addEventListener(target, 'pointerup', this._touchEnd);
    }
    SwipeHander.prototype._getTouchPosition = function (event) {
        return {
            x: event.pageX,
            y: event.pageY,
        };
    };
    SwipeHander.prototype._detectDirection = function (start, end) {
        var diff = start.x - end.x;
        if (diff > TRESHHOLD) {
            return ESwipeDirection.RIGHT;
        }
        else if (diff < -TRESHHOLD) {
            return ESwipeDirection.LEFT;
        }
        else {
            return ESwipeDirection.NONE;
        }
    };
    return SwipeHander;
}());
/* harmony default export */ const SwipeHandler = (SwipeHander);

;// CONCATENATED MODULE: ./src/tools/AsyncSleep.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (undefined && undefined.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var sleep = function (ms) {
    return __awaiter(this, void 0, void 0, function () {
        return __generator(this, function (_a) {
            return [2 /*return*/, new Promise(function (resolve, reject) {
                    setTimeout(function () {
                        resolve();
                    }, ms);
                })];
        });
    });
};
var sleepTillNextRenderFinished = function () {
    return __awaiter(this, void 0, void 0, function () {
        return __generator(this, function (_a) {
            return [2 /*return*/, new Promise(function (resolve, reject) {
                    requestAnimationFrame(function () {
                        // fires before next repaint
                        requestAnimationFrame(function () {
                            // fires before the _next_ next repaint
                            // ...which is effectively _after_ the next repaint
                            resolve();
                        });
                    });
                })];
        });
    });
};


;// CONCATENATED MODULE: ./src/components/ImageStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStage_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var ImageStage_generator = (undefined && undefined.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};





// constants
var ANIMATIONS_RESET_TIMEOUT = 1000;
/**
 * The ImageStage is responsible for proper scaling and centering
 * of images on the stage
 */
var ImageStage = /** @class */ (function () {
    function ImageStage(imageHandle, imageWidth, imageHeight) {
        this._zoomAnimation = false;
        this._imageHandle = imageHandle;
        this._imageWidth = imageWidth;
        this._imageHeight = imageHeight;
        this._imageStage = this._createStage();
    }
    ImageStage.prototype.setZoomAnimation = function (activate) {
        this._zoomAnimation = activate;
    };
    ImageStage.prototype.applyScaleMode = function () {
        console.log('ImageStage#applyScaleMode');
        var stageDimension = lib_getElementDimension(this._imageStage);
        this._applyScaleModeImpl(stageDimension.width, stageDimension.height);
        this._centerImage(stageDimension.width, stageDimension.height);
    };
    ImageStage.prototype.setSize = function (widthCss, heightCss) {
        lib_addCssStyle(this._imageStage, 'width', widthCss);
        lib_addCssStyle(this._imageStage, 'height', heightCss);
        this.applyScaleMode();
    };
    ImageStage.prototype.setMargin = function (marginCss) {
        lib_addCssStyle(this._imageStage, 'margin', marginCss);
    };
    ImageStage.prototype.hideImage = function (swipeDirection) {
        if (swipeDirection === void 0) { swipeDirection = ESwipeDirection.NONE; }
        return ImageStage_awaiter(this, void 0, void 0, function () {
            var _this = this;
            return ImageStage_generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (this._zoomAnimation) {
                            setTimeout(function () {
                                _this._resetZoom();
                            }, ANIMATIONS_RESET_TIMEOUT);
                        }
                        this._stopSlideAnimation();
                        return [4 /*yield*/, sleepTillNextRenderFinished()];
                    case 1:
                        _a.sent();
                        if (swipeDirection == ESwipeDirection.RIGHT) {
                            lib_addCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                            lib_addCssStyle(this._imageStage, 'left', '-100%');
                        }
                        else if (swipeDirection == ESwipeDirection.LEFT) {
                            lib_addCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                            lib_addCssStyle(this._imageStage, 'left', '100%');
                        }
                        removeCssStyle(this._imageStage, 'opacity');
                        return [2 /*return*/];
                }
            });
        });
    };
    ImageStage.prototype.showImage = function (swipeDirection) {
        if (swipeDirection === void 0) { swipeDirection = ESwipeDirection.NONE; }
        return ImageStage_awaiter(this, void 0, void 0, function () {
            return ImageStage_generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        this.applyScaleMode();
                        if (this._zoomAnimation) {
                            this._startZoomAnimation();
                        }
                        this._stopSlideAnimation();
                        return [4 /*yield*/, sleepTillNextRenderFinished()];
                    case 1:
                        _a.sent();
                        if (!(swipeDirection == ESwipeDirection.RIGHT)) return [3 /*break*/, 3];
                        removeCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                        lib_addCssStyle(this._imageStage, 'left', '100%');
                        return [4 /*yield*/, sleepTillNextRenderFinished()];
                    case 2:
                        _a.sent();
                        lib_addCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                        removeCssStyle(this._imageStage, 'left');
                        return [3 /*break*/, 6];
                    case 3:
                        if (!(swipeDirection == ESwipeDirection.LEFT)) return [3 /*break*/, 5];
                        removeCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                        lib_addCssStyle(this._imageStage, 'left', '-100%');
                        return [4 /*yield*/, sleepTillNextRenderFinished()];
                    case 4:
                        _a.sent();
                        lib_addCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                        removeCssStyle(this._imageStage, 'left');
                        return [3 /*break*/, 6];
                    case 5:
                        removeCssClass(this._imageStage, tools_animations_module.mibreit_GalleryTransition);
                        removeCssStyle(this._imageStage, 'left');
                        _a.label = 6;
                    case 6:
                        lib_addCssStyle(this._imageStage, 'opacity', '1');
                        return [2 /*return*/];
                }
            });
        });
    };
    ImageStage.prototype._createStage = function () {
        var wrapper = lib_createElement('div');
        lib_addCssClass(wrapper, components_ImageStage_module.mibreit_ImageStage);
        lib_addCssClass(wrapper, tools_animations_module.mibreit_GalleryFade);
        lib_wrapElements([this._imageHandle], wrapper);
        return wrapper;
    };
    ImageStage.prototype._centerImage = function (stageWidth, stageHeight) {
        var _a = lib_getElementDimension(this._imageHandle), width = _a.width, height = _a.height;
        var x = (width + stageWidth) / 2.0 - width;
        var y = (height + stageHeight) / 2.0 - height;
        lib_addCssStyle(this._imageHandle, 'margin-left', "".concat(x, "px"));
        lib_addCssStyle(this._imageHandle, 'margin-top', "".concat(y, "px"));
    };
    ImageStage.prototype._startZoomAnimation = function () {
        console.log('ImageStage#startZoomAnimation');
        lib_addCssClass(this._imageHandle, 'zoom');
    };
    ImageStage.prototype._resetZoom = function () {
        console.log('ImageStage#resetZoom');
        removeCssClass(this._imageHandle, 'zoom');
    };
    ImageStage.prototype._stopSlideAnimation = function () {
        lib_addCssStyle(this._imageStage, 'margin-left', getComputedCssStyle(this._imageStage, 'margin-left'));
    };
    return ImageStage;
}());
/* harmony default export */ const components_ImageStage = (ImageStage);

;// CONCATENATED MODULE: ./src/components/ImageStageFitAspect.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageFitAspect = /** @class */ (function (_super) {
    __extends(ImageStageFitAspect, _super);
    function ImageStageFitAspect(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageFitAspect.prototype._applyScaleModeImpl = function (stageWidth, stageHeight) {
        var aspect = this._imageWidth / this._imageHeight;
        var newWidth = 'auto';
        var newHeight = 'auto';
        if (stageWidth / stageHeight > aspect) {
            // fit based on height
            if (stageHeight <= this._imageHeight) {
                newHeight = "100%";
            }
        }
        else {
            // fit based on width
            if (stageWidth <= this._imageWidth) {
                newWidth = "100%";
            }
        }
        lib_addCssStyle(this._imageHandle, 'width', newWidth);
        lib_addCssStyle(this._imageHandle, 'height', newHeight);
    };
    return ImageStageFitAspect;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageFitAspect = (ImageStageFitAspect);

;// CONCATENATED MODULE: ./src/components/ImageStageExpand.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageExpand_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageExpand = /** @class */ (function (_super) {
    ImageStageExpand_extends(ImageStageExpand, _super);
    function ImageStageExpand(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageExpand.prototype._applyScaleModeImpl = function (stageWidth, stageHeight) {
        var aspect = this._imageWidth / this._imageHeight;
        var stageAspect = stageWidth / stageHeight;
        if (stageAspect > aspect) {
            // fit based on width
            lib_addCssStyle(this._imageHandle, 'width', "100%");
            lib_addCssStyle(this._imageHandle, 'height', "".concat((100 / aspect) * stageAspect, "%"));
        }
        else {
            // fit based on height
            lib_addCssStyle(this._imageHandle, 'width', "".concat((aspect * 100) / stageAspect, "%"));
            lib_addCssStyle(this._imageHandle, 'height', "100%");
        }
    };
    return ImageStageExpand;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageExpand = (ImageStageExpand);

;// CONCATENATED MODULE: ./src/components/ImageStageStretch.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageStretch_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageStretch = /** @class */ (function (_super) {
    ImageStageStretch_extends(ImageStageStretch, _super);
    function ImageStageStretch(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageStretch.prototype._applyScaleModeImpl = function (_stageWidth, _stageHeight) {
        lib_addCssStyle(this._imageHandle, 'width', "100%");
        lib_addCssStyle(this._imageHandle, 'height', "100%");
    };
    return ImageStageStretch;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageStretch = (ImageStageStretch);

;// CONCATENATED MODULE: ./src/components/ImageStageNoScale.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ImageStageNoScale_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var ImageStageNoScale = /** @class */ (function (_super) {
    ImageStageNoScale_extends(ImageStageNoScale, _super);
    function ImageStageNoScale(imageHandle, imageWidth, imageHeight) {
        return _super.call(this, imageHandle, imageWidth, imageHeight) || this;
    }
    ImageStageNoScale.prototype._applyScaleModeImpl = function (_stageWidth, _stageHeight) {
        removeCssStyle(this._imageHandle, 'width');
        removeCssStyle(this._imageHandle, 'height');
    };
    return ImageStageNoScale;
}(components_ImageStage));
/* harmony default export */ const components_ImageStageNoScale = (ImageStageNoScale);

;// CONCATENATED MODULE: ./src/factories/createImageStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */




var EImageScaleMode;
(function (EImageScaleMode) {
    EImageScaleMode[EImageScaleMode["NONE"] = 0] = "NONE";
    EImageScaleMode[EImageScaleMode["FIT_ASPECT"] = 1] = "FIT_ASPECT";
    EImageScaleMode[EImageScaleMode["STRETCH"] = 2] = "STRETCH";
    EImageScaleMode[EImageScaleMode["EXPAND"] = 3] = "EXPAND";
})(EImageScaleMode || (EImageScaleMode = {}));
/* harmony default export */ function createImageStage(imageHandle, imageWidth, imageHeight, scaleMode) {
    if (scaleMode === void 0) { scaleMode = EImageScaleMode.FIT_ASPECT; }
    switch (scaleMode) {
        case EImageScaleMode.EXPAND:
            return new components_ImageStageExpand(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.FIT_ASPECT:
            return new components_ImageStageFitAspect(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.STRETCH:
            return new components_ImageStageStretch(imageHandle, imageWidth, imageHeight);
        case EImageScaleMode.NONE:
        default:
            return new components_ImageStageNoScale(imageHandle, imageWidth, imageHeight);
    }
}
;

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/LazyLoader.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var LazyLoader_awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var LazyLoader_generator = (undefined && undefined.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var PRELOADER_WINDOW_SIZE = 5;
var LazyLoader_LazyLoader = /*@__PURE__*/ (function () {
    function LazyLoader(elementLoaders, preloaderBeforeSize, preloaderAfterSize) {
        if (preloaderBeforeSize === void 0) { preloaderBeforeSize = 0; }
        if (preloaderAfterSize === void 0) { preloaderAfterSize = PRELOADER_WINDOW_SIZE; }
        this._unloadedElementIndices = [];
        this._currentIndex = -1;
        this._elementLoaders = elementLoaders;
        this._preloaderBeforeSize = preloaderBeforeSize;
        this._preloaderAfterSize = preloaderAfterSize;
        this._updateUnloadedElementIndices();
    }
    LazyLoader.prototype.loadAll = function () {
        this._loadElements(0, this._elementLoaders.length);
    };
    LazyLoader.prototype.setCurrentIndex = function (newIndex) {
        if (this._currentIndex != newIndex) {
            this._currentIndex = newIndex;
            this._moveWindow();
        }
    };
    /**
     * Will directly load an Element without changing the window of loaded Elements. This
     * has to be handled separately by setCurrentIndex
     *
     * @return {Promise} Promise that will resolve with true once the Element is loaded
     *         or will resolve right away with false, if the Element is in loading state;
     *         It will reject an invalid index with a error message
     */
    LazyLoader.prototype.loadElement = function (index) {
        return LazyLoader_awaiter(this, void 0, void 0, function () {
            var loaded, location_1, wasLoaded_1;
            return LazyLoader_generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!(index >= 0 && index < this._elementLoaders.length)) return [3 /*break*/, 6];
                        loaded = false;
                        if (!!this._unloadedElementIndices.includes(index)) return [3 /*break*/, 1];
                        loaded = true;
                        return [3 /*break*/, 5];
                    case 1:
                        console.log("LazyLoader#loadElement - index: ", index);
                        _a.label = 2;
                    case 2:
                        _a.trys.push([2, 4, , 5]);
                        return [4 /*yield*/, this._elementLoaders[index].load()];
                    case 3:
                        loaded = _a.sent();
                        if (loaded) {
                            location_1 = this._unloadedElementIndices.indexOf(index);
                            if (location_1 > -1) {
                                this._unloadedElementIndices.splice(location_1, 1);
                            }
                        }
                        return [3 /*break*/, 5];
                    case 4:
                        wasLoaded_1 = _a.sent();
                        loaded = wasLoaded_1;
                        return [3 /*break*/, 5];
                    case 5: return [2 /*return*/, loaded];
                    case 6: throw new Error("Preloader#loadElement -> invalid Index ".concat(index));
                }
            });
        });
    };
    LazyLoader.prototype.getElementLoaderInfos = function () {
        return this._elementLoaders;
    };
    LazyLoader.prototype.getUnloadedElementIndices = function () {
        return this._unloadedElementIndices;
    };
    LazyLoader.prototype._moveWindow = function () {
        if (this._unloadedElementIndices.length) {
            var start = this._currentIndex - this._preloaderBeforeSize;
            var end = this._currentIndex + this._preloaderAfterSize;
            // 1) load from current Element forward
            this._loadElements(this._currentIndex, end < this._elementLoaders.length ? end : this._elementLoaders.length);
            // 2) load from back towards current Element
            this._loadElements(start >= 0 ? start : 0, this._currentIndex);
            // 3) handle overflow
            if (start < 0) {
                start = this._elementLoaders.length + start;
                this._loadElements(start, this._elementLoaders.length);
            }
            if (end >= this._elementLoaders.length) {
                end = end - this._elementLoaders.length;
                this._loadElements(0, end);
            }
        }
    };
    LazyLoader.prototype._loadElements = function (start, end) {
        for (var i = start < 0 ? 0 : start; i < end && i < this._elementLoaders.length; i++) {
            this.loadElement(i);
        }
    };
    LazyLoader.prototype._updateUnloadedElementIndices = function () {
        for (var i = 0; i < this._elementLoaders.length; i++) {
            if (!this._elementLoaders[i].wasLoaded()) {
                this._unloadedElementIndices.push(i);
            }
        }
    };
    return LazyLoader;
}());
/* harmony default export */ const components_LazyLoader = (LazyLoader_LazyLoader);

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/ElementLoader.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var LAZY_LOADING_CLASS = 'mibreit-LazyLoader_lazy';
var LAZY_LOADING_ANIMATE_CLASS = 'mibreit-LazyLoader_ElementLoader_animate';
var DATA_SRC_ATTRIBUTE = 'data-src';
var SRC_ATTRIBUTE = 'src';
var EImageState;
(function (EImageState) {
    EImageState[EImageState["INACTIVE"] = 0] = "INACTIVE";
    EImageState[EImageState["LOADING"] = 1] = "LOADING";
    EImageState[EImageState["LOADED"] = 2] = "LOADED";
})(EImageState || (EImageState = {}));
var ElementLoader = /*@__PURE__*/ (function () {
    function ElementLoader(element) {
        var _a;
        this._state = EImageState.INACTIVE;
        this._wasLoadedCallbacks = new Array();
        this._dataSrc = null;
        this._element = element;
        this._dataSrc = getAttribute(element, DATA_SRC_ATTRIBUTE);
        var classes = (_a = getCssClasses(element)) === null || _a === void 0 ? void 0 : _a.split(',');
        if (this._dataSrc || (classes && classes.includes(LAZY_LOADING_CLASS))) {
            this._state = EImageState.INACTIVE;
            this._setBaseStyle();
        }
        else {
            this._state = EImageState.LOADED;
        }
        console.log('ElementLoader#constructor -> state', this._state);
    }
    ElementLoader.prototype.load = function () {
        var _this = this;
        console.log('ElementLoader#load');
        return new Promise(function (resolve, reject) {
            if (_this._state == EImageState.INACTIVE) {
                _this._element.onload = function () {
                    _this._finishLoad();
                    resolve(true);
                };
                _this._triggerLoad();
                _this._state = EImageState.LOADING;
                _this._setLoadingStyle();
                if (_this._element instanceof HTMLImageElement && _this._element.complete) {
                    // images that have been cached don't trigger the onload event, so we manually trigger it
                    _this._element.onload = null;
                    _this._finishLoad();
                    resolve(true);
                }
            }
            else if (_this._state === EImageState.LOADING) {
                reject(false);
            }
            else {
                reject(true);
            }
        });
    };
    ElementLoader.prototype.wasLoaded = function () {
        return this._state === EImageState.LOADED;
    };
    ElementLoader.prototype.addWasLoadedCallback = function (callback) {
        if (!this._wasLoadedCallbacks.includes(callback)) {
            this._wasLoadedCallbacks.push(callback);
        }
    };
    ElementLoader.prototype._triggerLoad = function () {
        if (this._dataSrc) {
            setAttribute(this._element, SRC_ATTRIBUTE, this._dataSrc);
        }
        removeCssClass(this._element, LAZY_LOADING_CLASS);
    };
    ElementLoader.prototype._finishLoad = function () {
        var _this = this;
        console.log('ElementLoader#_finishLoad');
        removeAttribute(this._element, DATA_SRC_ATTRIBUTE);
        this._state = EImageState.LOADED;
        this._wasLoadedCallbacks.forEach(function (callback) {
            callback();
        });
        // A little explanation for the timeout: we need to decouple the
        // change of the load style and the fade it will trigger from the
        // change to the dom that might be triggered by the callbacks
        // -> this needs to happen separately and a timeout helps to provide
        // this separation
        setTimeout(function () {
            _this._setLoadedStyle();
        }, 50);
    };
    ElementLoader.prototype._setBaseStyle = function () {
        lib_addCssStyle(this._element, 'opacity', '0');
    };
    ElementLoader.prototype._setLoadingStyle = function () {
        lib_addCssClass(this._element, LAZY_LOADING_ANIMATE_CLASS);
    };
    ElementLoader.prototype._setLoadedStyle = function () {
        var _this = this;
        removeCssStyle(this._element, 'opacity');
        setTimeout(function () {
            removeCssClass(_this._element, LAZY_LOADING_ANIMATE_CLASS);
        }, 1000);
    };
    return ElementLoader;
}());
/* harmony default export */ const components_ElementLoader = (ElementLoader);

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/ElementInfo.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var ElementInfo = /*@__PURE__*/ (function () {
    function ElementInfo(element) {
        var width = getAttribute(element, 'width');
        var height = getAttribute(element, 'height');
        if (width == null || height == null) {
            throw new Error('ElementInfo#constructor - supported elements need a width and height property');
        }
        var originalStyle = getAttribute(element, 'class');
        this._element = element;
        this._originalElementStyle = originalStyle ? originalStyle : '';
        this._width = parseInt(width);
        this._height = parseInt(height);
    }
    ElementInfo.prototype.getWidth = function () {
        return this._width;
    };
    ElementInfo.prototype.getHeight = function () {
        return this._height;
    };
    ElementInfo.prototype.getStyle = function () {
        return this._originalElementStyle;
    };
    ElementInfo.prototype.isElementWithinScrollArea = function () {
        return lib_isElementWithinWindow(this._element);
    };
    return ElementInfo;
}());
/* harmony default export */ const components_ElementInfo = (ElementInfo);

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/Element.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var Element_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


var Element_Element = /*@__PURE__*/ (function (_super) {
    Element_extends(Element, _super);
    function Element(element) {
        var _this = _super.call(this, element) || this;
        _this._elementInfo = new components_ElementInfo(_this._element);
        return _this;
    }
    Element.prototype.getHtmlElement = function () {
        return this._element;
    };
    Element.prototype.getWidth = function () {
        return this._elementInfo.getWidth();
    };
    Element.prototype.getHeight = function () {
        return this._elementInfo.getHeight();
    };
    Element.prototype.getStyle = function () {
        return this._elementInfo.getStyle();
    };
    Element.prototype.isElementWithinScrollArea = function () {
        return this._elementInfo.isElementWithinScrollArea();
    };
    return Element;
}(components_ElementLoader));
/* harmony default export */ const components_Element = (Element_Element);

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/ElementSurrogate.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var ElementSurrogate_ElementSurrogate = /*@__PURE__*/ ((/* unused pure expression or super */ null && (function () {
    function ElementSurrogate(element, horizontal) {
        if (horizontal === void 0) { horizontal = false; }
        var _this = this;
        this._element = element;
        this._surrogate = this._createSurrogate(horizontal);
        wrapElements([element.getHtmlElement()], this._surrogate);
        element.addWasLoadedCallback(function () {
            unwrapElements(_this._surrogate);
            removeElement(_this._surrogate);
        });
    }
    ElementSurrogate.prototype.getWidth = function () {
        return this._element.getWidth();
    };
    ElementSurrogate.prototype.getHeight = function () {
        return this._element.getHeight();
    };
    ElementSurrogate.prototype.getStyle = function () {
        return this._element.getStyle();
    };
    ElementSurrogate.prototype.isElementWithinScrollArea = function () {
        return isElementWithinWindow(this._surrogate);
    };
    ElementSurrogate.prototype._resize = function (horizontal) {
        console.log('ElementSurrogate#resize', getElementDimension(this._surrogate).height, this._element.getWidth(), this._element.getHeight());
        if (horizontal) {
            addCssStyle(this._surrogate, 'width', "".concat((getElementDimension(this._surrogate).height * this._element.getWidth()) / this._element.getHeight(), "px"));
        }
        // else nothing to do for now
    };
    ElementSurrogate.prototype._createSurrogate = function (horizontal) {
        var _this = this;
        if (horizontal === void 0) { horizontal = false; }
        console.log('ElementSurrogate#_createSurrogate', horizontal);
        var surrogate = createElement('div');
        addCssClass(surrogate, 'mibreit_lazyLoader_surrogate');
        if (horizontal) {
            overwriteCssStyles(surrogate, "overflow: hidden; height: 100%; width: ".concat((getElementDimension(this._element.getHtmlElement()).height * this._element.getWidth()) /
                this._element.getHeight(), "px; flex-shrink:0;"));
            setTimeout(function () {
                _this._resize(true);
            }, 0);
        }
        else {
            overwriteCssStyles(surrogate, "overflow: hidden; width: 100%; height: 0; padding-bottom: ".concat((this._element.getHeight() * 100) / this._element.getWidth(), "%;"));
        }
        return surrogate;
    };
    return ElementSurrogate;
}())));
/* harmony default export */ const components_ElementSurrogate = ((/* unused pure expression or super */ null && (ElementSurrogate_ElementSurrogate)));

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/components/ScrollLoader.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */


// avoid overloading of scroll event
var SCROLL_EVENT_TIMEOUT = 400;
var ScrollLoader_ScrollLoader = /*@__PURE__*/ ((/* unused pure expression or super */ null && (function () {
    function ScrollLoader(lazyLoader, elementLocations) {
        this._elementLocations = [];
        this._lazyLoader = lazyLoader;
        this._elementLocations = elementLocations;
    }
    ScrollLoader.prototype.startLoader = function () {
        var _this = this;
        this._loadElementsWithinWindowRect();
        var debouncedLoadEvent = debounce(function () {
            _this._loadElementsWithinWindowRect();
        }, SCROLL_EVENT_TIMEOUT);
        addScrollEventListener(function (_event) {
            debouncedLoadEvent();
        });
    };
    ScrollLoader.prototype._loadElementsWithinWindowRect = function () {
        var unloadedElementIndices = this._lazyLoader.getUnloadedElementIndices();
        console.log('ScrollLoader#loadElementsWithinWindowRect - unloadedElements: ', unloadedElementIndices);
        for (var i = 0; i < unloadedElementIndices.length; i++) {
            if (this._elementLocations[unloadedElementIndices[i]].isElementWithinScrollArea()) {
                this._lazyLoader.loadElement(unloadedElementIndices[i]);
            }
        }
        // TODO: Further optimization -> stop once we leave the window area
    };
    return ScrollLoader;
}())));
/* harmony default export */ const components_ScrollLoader = ((/* unused pure expression or super */ null && (ScrollLoader_ScrollLoader)));

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/tools/createLazyLoader.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */





var ELazyMode;
(function (ELazyMode) {
    ELazyMode[ELazyMode["SIMPLE_DEFER"] = 0] = "SIMPLE_DEFER";
    ELazyMode[ELazyMode["WINDOWED_EXTERNAL"] = 1] = "WINDOWED_EXTERNAL";
    ELazyMode[ELazyMode["WINDOWED_SCROLL"] = 2] = "WINDOWED_SCROLL";
    ELazyMode[ELazyMode["WINDOWED_SCROLL_HORIZONTAL"] = 3] = "WINDOWED_SCROLL_HORIZONTAL";
})(ELazyMode || (ELazyMode = {}));
function checkElementSelectorInput(elementSelector) {
    if (typeof elementSelector !== 'string') {
        throw new Error('createLazyLoader - first parameter needs to be a css selector string');
    }
}
function checkConfig(config) {
    if (typeof config.preloaderAfterSize !== 'undefined' && typeof config.preloaderAfterSize !== 'number') {
        throw new Error('createLazyLoader - preloaderAfterSize of config must be a number');
    }
    if (typeof config.preloaderBeforeSize !== 'undefined' && typeof config.preloaderBeforeSize !== 'number') {
        throw new Error('createLazyLoader - preloaderBeforeSize of config must be a number');
    }
    if (typeof config.mode !== 'undefined' && typeof config.mode !== 'number') {
        throw new Error('createLazyLoader - mode of config must be a number (0, 1, 2, 3) - use type ELazyMode');
    }
}
function startLoader(loader, mode, elementLocations) {
    if (mode === void 0) { mode = ELazyMode.SIMPLE_DEFER; }
    console.log('startLoader');
    if (mode != null) {
        switch (mode) {
            case ELazyMode.WINDOWED_EXTERNAL:
                loader.loadElement(0);
                loader.setCurrentIndex(0);
                break;
            case ELazyMode.WINDOWED_SCROLL:
            case ELazyMode.WINDOWED_SCROLL_HORIZONTAL:
                var scrollLoader = new ScrollLoader(loader, elementLocations);
                scrollLoader.startLoader();
                break;
            case ELazyMode.SIMPLE_DEFER:
            default:
                loader.loadAll();
                break;
        }
    }
    else {
        loader.loadAll();
    }
}
function createLazyLoaderFromElements(elements, config) {
    checkConfig(config);
    console.log('createLazyLoaderFromElements', JSON.stringify(config));
    var elementLocations = elements;
    if (config === null || config === void 0 ? void 0 : config.useSurrogate) {
        var surrogates = [];
        if (config.mode === ELazyMode.WINDOWED_SCROLL || config.mode === ELazyMode.WINDOWED_SCROLL_HORIZONTAL) {
            for (var i = 0; i < elements.length; i++) {
                var surrogate = new ElementSurrogate(elements[i], config.mode === ELazyMode.WINDOWED_SCROLL_HORIZONTAL ? true : false);
                surrogates.push(surrogate);
            }
        }
        elementLocations = surrogates;
    }
    var lazyLoader = new LazyLoader(elements, config.preloaderBeforeSize, config.preloaderAfterSize);
    // enqueing execution of startLoader -> to ensure that resize events from Surrogates are executed before
    setTimeout(function () {
        startLoader(lazyLoader, config.mode, elementLocations);
    }, 0);
    return lazyLoader;
}
function createLazyLoader(elementSelector, config) {
    checkElementSelectorInput(elementSelector);
    console.log('createLazyLoader', elementSelector, JSON.stringify(config));
    var htmlElements = getElements(elementSelector);
    var elements = [];
    for (var i = 0; i < htmlElements.length; i++) {
        var element = new Element(htmlElements[i]);
        elements.push(element);
    }
    return createLazyLoaderFromElements(elements, config);
}

;// CONCATENATED MODULE: ./node_modules/mibreit-lazy-loader/lib/index.js
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */






;// CONCATENATED MODULE: ./src/constants.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
// Css properties
var IMAGE_DATA_SRC_ATTRIBUTE = 'data-src';
var IMAGE_SRC_ATTRIBUTE = 'src';
var IMAGE_TITLE_ATTRIBUTE = 'title';
var IMAGE_DATA_TITLE_ATTRIBUTE = 'data-title';
// Css classes
var CSS_IMAGE_STATE_CLASS = 'mibreit-image-stage';
// Gallery Configuration
var PRELOADER_RIGHT_SIZE = 7;
var PRELOADER_LEFT_SIZE = 3;
var SLIDESHOW_INTERVAL = 5000;
var NUMBER_OF_VISIBLE_THUMBS = 7;
// Opacities
var GALLERY_BUTTONS_SHOW_OPACITY = 0.5;

;// CONCATENATED MODULE: ./src/components/Image.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var Image_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


// constants

var Image = /** @class */ (function (_super) {
    Image_extends(Image, _super);
    function Image(imageHandle) {
        var _this = _super.call(this, imageHandle) || this;
        _this._title = '';
        _this._prepareTitle(imageHandle);
        _this._limitMaxSizeTo(imageHandle, _this.getWidth(), _this.getHeight());
        disableContextMenu(imageHandle);
        disableDragging(imageHandle);
        return _this;
    }
    Image.prototype.getTitle = function () {
        return this._title;
    };
    Image.prototype.getUrl = function () {
        return this._element.hasAttribute('data-src')
            ? this._element.getAttribute('data-src')
            : this._element.getAttribute('src');
    };
    Image.prototype._limitMaxSizeTo = function (imageHandle, maxWidth, maxHeight) {
        lib_overwriteCssStyles(imageHandle, "max-width: ".concat(maxWidth, "px; max-height: ").concat(maxHeight, "px"));
    };
    Image.prototype._prepareTitle = function (imageHandle) {
        var title = imageHandle.getAttribute(IMAGE_TITLE_ATTRIBUTE);
        if (title) {
            imageHandle.setAttribute(IMAGE_DATA_TITLE_ATTRIBUTE, title);
            imageHandle.removeAttribute(IMAGE_TITLE_ATTRIBUTE);
        }
        else {
            title = imageHandle.getAttribute(IMAGE_DATA_TITLE_ATTRIBUTE);
        }
        this._title = title ? title : '';
    };
    return Image;
}(components_Element));
/* harmony default export */ const components_Image = (Image);

;// CONCATENATED MODULE: ./src/components/ImageViewer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

// types

var ImageViewer = /** @class */ (function () {
    function ImageViewer(images, scaleMode) {
        if (scaleMode === void 0) { scaleMode = EImageScaleMode.FIT_ASPECT; }
        this._currentIndex = -1;
        this._imageStages = [];
        this._imageChangedCallbacks = new Array();
        this._images = images;
        this._prepareImageStages(images, scaleMode);
    }
    ImageViewer.prototype.showImage = function (index) {
        return this._showImage(index);
    };
    ImageViewer.prototype.showNextImage = function (swipeDirection) {
        var newIndex = this._currentIndex < this._imageStages.length - 1 ? this._currentIndex + 1 : 0;
        return this._showImage(newIndex, swipeDirection);
    };
    ImageViewer.prototype.showPreviousImage = function (swipeDirection) {
        var newIndex = this._currentIndex > 0 ? this._currentIndex - 1 : this._imageStages.length - 1;
        return this._showImage(newIndex, swipeDirection);
    };
    ImageViewer.prototype.getNumberOfImages = function () {
        return this._imageStages.length;
    };
    ImageViewer.prototype.reinitSize = function () {
        if (this._isValidIndex(this._currentIndex)) {
            this._imageStages[this._currentIndex].applyScaleMode();
        }
    };
    ImageViewer.prototype.setZoomAnimation = function (active) {
        this._imageStages.forEach(function (stage) {
            stage.setZoomAnimation(active);
        });
    };
    ImageViewer.prototype.addImageChangedCallback = function (callback) {
        if (!this._imageChangedCallbacks.includes(callback)) {
            this._imageChangedCallbacks.push(callback);
        }
    };
    ImageViewer.prototype.getImageIndex = function () {
        return this._currentIndex;
    };
    ImageViewer.prototype.getImageInfo = function (index) {
        if (this._isValidIndex(index)) {
            return this._images[index];
        }
        else {
            return null;
        }
    };
    ImageViewer.prototype.getImageElement = function (index) {
        if (this._isValidIndex(index)) {
            return this._images[index].getHtmlElement();
        }
        else {
            return null;
        }
    };
    ImageViewer.prototype._showImage = function (index, swipeDirection) {
        var _this = this;
        if (swipeDirection === void 0) { swipeDirection = ESwipeDirection.NONE; }
        if (this._isValidIndex(index)) {
            if (index != this._currentIndex) {
                if (!this._images[index].wasLoaded()) {
                    this._images[index].addWasLoadedCallback(function () {
                        _this._hideImage(_this._currentIndex, swipeDirection);
                        _this._changeCurrentImage(index, swipeDirection);
                    });
                }
                else {
                    this._hideImage(this._currentIndex, swipeDirection);
                    this._changeCurrentImage(index, swipeDirection);
                }
            }
            return true;
        }
        else {
            return false;
        }
    };
    ImageViewer.prototype._hideImage = function (index, swipeDirection) {
        if (swipeDirection === void 0) { swipeDirection = ESwipeDirection.NONE; }
        if (this._isValidIndex(index)) {
            this._imageStages[this._currentIndex].hideImage(swipeDirection);
        }
    };
    ImageViewer.prototype._prepareImageStages = function (images, scaleMode) {
        var _this = this;
        images.forEach(function (image) {
            var imageStage = createImageStage(image.getHtmlElement(), image.getWidth(), image.getHeight(), scaleMode);
            image.addWasLoadedCallback(function () {
                imageStage.applyScaleMode();
            });
            _this._imageStages.push(imageStage);
        });
    };
    ImageViewer.prototype._isValidIndex = function (index) {
        return index >= 0 && index < this._imageStages.length;
    };
    ImageViewer.prototype._changeCurrentImage = function (index, swipeDirection) {
        var _this = this;
        if (swipeDirection === void 0) { swipeDirection = ESwipeDirection.NONE; }
        console.log('ImageViewer#changeCurrentImage', index);
        this._currentIndex = index;
        this._imageStages[this._currentIndex].showImage(swipeDirection);
        this._imageChangedCallbacks.forEach(function (callback) {
            callback(_this._currentIndex, _this._images[_this._currentIndex]);
        });
    };
    return ImageViewer;
}());
/* harmony default export */ const components_ImageViewer = (ImageViewer);

;// CONCATENATED MODULE: ./src/containers/SlideshowContainer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

// helpers

// constants


var SlideshowContainer = /** @class */ (function () {
    function SlideshowContainer(elements, config) {
        var _this = this;
        var images = this._prepareImages(elements);
        this._loader = this._prepareLoader(images, config === null || config === void 0 ? void 0 : config.preloaderBeforeSize, config === null || config === void 0 ? void 0 : config.preloaderAfterSize);
        this._imageViewer = this._prepareImageViewer(images, this._loader, config === null || config === void 0 ? void 0 : config.scaleMode, config === null || config === void 0 ? void 0 : config.zoom);
        if (config === null || config === void 0 ? void 0 : config.interval) {
            setInterval(function () {
                _this._imageViewer.showNextImage();
            }, config.interval);
        }
        this._imageViewer.showImage(0);
    }
    SlideshowContainer.prototype.getViewer = function () {
        return this._imageViewer;
    };
    SlideshowContainer.prototype.getLoader = function () {
        return this._loader;
    };
    SlideshowContainer.prototype._prepareImages = function (imagesSelector) {
        var images = new Array();
        for (var i = 0; i < imagesSelector.length; i++) {
            var image = new components_Image(imagesSelector[i]);
            images.push(image);
        }
        return images;
    };
    SlideshowContainer.prototype._prepareLoader = function (images, preloaderBeforeSize, preloaderAfterSize) {
        if (preloaderBeforeSize === void 0) { preloaderBeforeSize = PRELOADER_LEFT_SIZE; }
        if (preloaderAfterSize === void 0) { preloaderAfterSize = PRELOADER_RIGHT_SIZE; }
        var lazyLoader = new components_LazyLoader(images, preloaderBeforeSize, preloaderAfterSize);
        setTimeout(function () {
            lazyLoader.loadElement(0);
            lazyLoader.setCurrentIndex(0);
        }, 0);
        return lazyLoader;
    };
    SlideshowContainer.prototype._prepareImageViewer = function (images, loader, scaleMode, zoom) {
        var imageViewer = new components_ImageViewer(images, scaleMode);
        if (zoom !== undefined) {
            imageViewer.setZoomAnimation(zoom);
        }
        if (loader) {
            imageViewer.addImageChangedCallback(function (index, _imageInfo) {
                loader.setCurrentIndex(index);
            });
            loader.setCurrentIndex(0);
        }
        return imageViewer;
    };
    return SlideshowContainer;
}());
/* harmony default export */ const containers_SlideshowContainer = (SlideshowContainer);

;// CONCATENATED MODULE: ./src/tools/checkSlideshowConfig.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
function checkSlideshowConfig(config) {
    if (typeof config.scaleMode !== 'undefined' &&
        (typeof config.scaleMode !== 'number' || config.scaleMode < 0 || config.scaleMode > 3)) {
        throw new Error('checkSlideshowConfig - config scaleMode should be of type EImageScaleMode (number 0 - 3)');
    }
    if (typeof config.interval !== 'undefined' && (typeof config.interval !== 'number' || config.interval < 1000)) {
        throw new Error('checkSlideshowConfig - config interval should be of type number and larger than 1000');
    }
    if (typeof config.zoom !== 'undefined' && typeof config.zoom !== 'boolean') {
        throw new Error('checkSlideshowConfig - config zoom should be of type boolean');
    }
    if (typeof config.preloaderAfterSize !== 'undefined' &&
        (typeof config.preloaderAfterSize !== 'number' || config.preloaderAfterSize < 0)) {
        throw new Error('checkSlideshowConfig - config preloaderAfterSize should be of type number');
    }
    if (typeof config.preloaderBeforeSize !== 'undefined' &&
        (typeof config.preloaderBeforeSize !== 'number' || config.preloaderBeforeSize < 0)) {
        throw new Error('checkSlideshowConfig - config preloaderBeforeSize should be of type number');
    }
}

;// CONCATENATED MODULE: ./src/factories/createSlideshow.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



/* harmony default export */ function createSlideshow(imageSelector, config) {
    if (typeof imageSelector !== 'string') {
        throw new Error('createSlideshow - second parameter must be imageSelector string');
    }
    var elements = lib_getElements(imageSelector);
    if ((elements === null || elements === void 0 ? void 0 : elements.length) > 0) {
        if (config) {
            checkSlideshowConfig(config);
        }
        return new containers_SlideshowContainer(elements, config);
    }
    else {
        throw new Error('createSlideshow - no images selected');
    }
}

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ThumbStage.module.css
var ThumbStage_module = __webpack_require__(867);
;// CONCATENATED MODULE: ./src/components/ThumbStage.module.css

      
      
      
      
      
      
      
      
      

var ThumbStage_module_options = {};

;
ThumbStage_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      ThumbStage_module_options.insert = insertBySelector_default().bind(null, "head");
    
ThumbStage_module_options.domAPI = (singletonStyleDomAPI_default());
ThumbStage_module_options.insertStyleElement = (insertStyleElement_default());

var ThumbStage_module_update = injectStylesIntoStyleTag_default()(ThumbStage_module/* default */.Z, ThumbStage_module_options);




       /* harmony default export */ const components_ThumbStage_module = (ThumbStage_module/* default */.Z && ThumbStage_module/* default */.Z.locals ? ThumbStage_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/components/ThumbStage.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var ThumbStage_extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();



var ThumbStage = /** @class */ (function (_super) {
    ThumbStage_extends(ThumbStage, _super);
    function ThumbStage(imageHandle, imageWidth, imageHeight) {
        var _this = _super.call(this, imageHandle, imageWidth, imageHeight) || this;
        lib_addCssClass(_this._imageStage, components_ThumbStage_module.mibreit_ThumbStage);
        return _this;
    }
    ThumbStage.prototype.addStageClickedCallback = function (callback) {
        addClickEventListener(this._imageStage, callback);
    };
    return ThumbStage;
}(components_ImageStageExpand));
/* harmony default export */ const components_ThumbStage = (ThumbStage);

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/ThumbScrollerLayout.module.css
var ThumbScrollerLayout_module = __webpack_require__(136);
;// CONCATENATED MODULE: ./src/components/ThumbScrollerLayout.module.css

      
      
      
      
      
      
      
      
      

var ThumbScrollerLayout_module_options = {};

;
ThumbScrollerLayout_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      ThumbScrollerLayout_module_options.insert = insertBySelector_default().bind(null, "head");
    
ThumbScrollerLayout_module_options.domAPI = (singletonStyleDomAPI_default());
ThumbScrollerLayout_module_options.insertStyleElement = (insertStyleElement_default());

var ThumbScrollerLayout_module_update = injectStylesIntoStyleTag_default()(ThumbScrollerLayout_module/* default */.Z, ThumbScrollerLayout_module_options);




       /* harmony default export */ const components_ThumbScrollerLayout_module = (ThumbScrollerLayout_module/* default */.Z && ThumbScrollerLayout_module/* default */.Z.locals ? ThumbScrollerLayout_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/images/nextThumbs.svg
/* harmony default export */ const nextThumbs = ("<svg viewBox=\"0 0 1792 1792\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M1152 896q0 26-19 45l-448 448q-19 19-45 19t-45-19-19-45v-896q0-26 19-45t45-19 45 19l448 448q19 19 19 45z\"/></svg>");
;// CONCATENATED MODULE: ./src/components/ThumbScrollerLayout.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



var ThumbScrollerLayout = /** @class */ (function () {
    function ThumbScrollerLayout(container, thumbStages, numberOfVisibleThumbs) {
        lib_addCssClass(container, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerParentContainer);
        this._numberOfVisibleThumbs = numberOfVisibleThumbs;
        this._thumbStages = thumbStages;
        var willThumbsFitContainer = numberOfVisibleThumbs >= thumbStages.length;
        this._scrollerContainer = this._createScrollerContainer(container, willThumbsFitContainer);
        var _a = this._createScrollerButtons(container, willThumbsFitContainer), previousButton = _a[0], nextButton = _a[1];
        this._previousButton = previousButton;
        this._nextButton = nextButton;
        this._thumbSizeRem = this._calculateThumbsize(this._scrollerContainer, numberOfVisibleThumbs);
        this._resizeThumbStages(this._thumbSizeRem);
    }
    ThumbScrollerLayout.prototype.reinitSize = function () {
        this._thumbSizeRem = this._calculateThumbsize(this._scrollerContainer, this._numberOfVisibleThumbs);
        this._resizeThumbStages(this._thumbSizeRem);
    };
    ThumbScrollerLayout.prototype.getThumbSizeRem = function () {
        return this._thumbSizeRem;
    };
    ThumbScrollerLayout.prototype.getNumberOfVisibleThumbs = function () {
        return this._numberOfVisibleThumbs;
    };
    ThumbScrollerLayout.prototype.getNumberOfThumbs = function () {
        return this._thumbStages.length;
    };
    ThumbScrollerLayout.prototype.getThumbScrollerButtons = function () {
        return { previousButton: this._previousButton, nextButton: this._nextButton };
    };
    ThumbScrollerLayout.prototype.getScrollerContainer = function () {
        return this._scrollerContainer;
    };
    ThumbScrollerLayout.prototype._createScrollerContainer = function (container, centerThumbs) {
        var childNodes = getChildNodes(container);
        var scrollerContainer = lib_createElement('div');
        if (centerThumbs) {
            lib_addCssClass(scrollerContainer, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerContainerCentered);
        }
        else {
            lib_addCssClass(scrollerContainer, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerContainer);
        }
        lib_wrapElements(childNodes, scrollerContainer);
        return scrollerContainer;
    };
    ThumbScrollerLayout.prototype._calculateThumbsize = function (container, numberOfVisibleThumbs) {
        var oneRemSize = getRootFontSize();
        var containerWidthRem = lib_getElementDimension(container).width / oneRemSize;
        var thumbsize = containerWidthRem / numberOfVisibleThumbs;
        return thumbsize;
    };
    ThumbScrollerLayout.prototype._createScrollerButtons = function (container, hidden) {
        var previousButton = lib_createElement('div');
        setInnerHtml(previousButton, nextThumbs);
        lib_addCssClass(previousButton, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerPrevious);
        prependChildElement(previousButton, container);
        var nextButton = lib_createElement('div');
        setInnerHtml(nextButton, nextThumbs);
        lib_addCssClass(nextButton, components_ThumbScrollerLayout_module.mibreit_ThumbScrollerNext);
        appendChildElement(nextButton, container);
        if (hidden) {
            lib_addCssStyle(previousButton, 'opacity', '0');
            lib_addCssStyle(nextButton, 'opacity', '0');
        }
        return [previousButton, nextButton];
    };
    ThumbScrollerLayout.prototype._resizeThumbStages = function (size) {
        var innerSize = size * 0.9;
        var margin = size * 0.05;
        this._thumbStages.forEach(function (thumbStage) {
            thumbStage.setSize("".concat(innerSize, "rem"), "".concat(innerSize, "rem"));
            thumbStage.setMargin("".concat(margin, "rem"));
        });
    };
    return ThumbScrollerLayout;
}());
/* harmony default export */ const components_ThumbScrollerLayout = (ThumbScrollerLayout);

;// CONCATENATED MODULE: ./src/factories/createThumbScrollerLayout.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



function prepareThumbStages(thumbs, thumbClickedCallback) {
    var thumbStages = new Array();
    thumbs.forEach(function (thumb, index) {
        var thumbStage = new components_ThumbStage(thumb.getHtmlElement(), thumb.getWidth(), thumb.getHeight());
        thumb.addWasLoadedCallback(function () {
            thumbStage.applyScaleMode();
        });
        thumbStage.showImage();
        if (thumbClickedCallback) {
            thumbStage.addStageClickedCallback(function () {
                thumbClickedCallback(index);
            });
        }
        thumbStages.push(thumbStage);
    });
    return thumbStages;
}
/* harmony default export */ function createThumbScrollerLayout(container, thumbs, numberOfVisibleThumbs, thumbClickedCallback) {
    if (numberOfVisibleThumbs === void 0) { numberOfVisibleThumbs = NUMBER_OF_VISIBLE_THUMBS; }
    var thumbStages = prepareThumbStages(thumbs, thumbClickedCallback);
    return new components_ThumbScrollerLayout(container, thumbStages, numberOfVisibleThumbs);
}

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/components/HorizontalScroller.module.css
var HorizontalScroller_module = __webpack_require__(706);
;// CONCATENATED MODULE: ./src/components/HorizontalScroller.module.css

      
      
      
      
      
      
      
      
      

var HorizontalScroller_module_options = {};

;
HorizontalScroller_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      HorizontalScroller_module_options.insert = insertBySelector_default().bind(null, "head");
    
HorizontalScroller_module_options.domAPI = (singletonStyleDomAPI_default());
HorizontalScroller_module_options.insertStyleElement = (insertStyleElement_default());

var HorizontalScroller_module_update = injectStylesIntoStyleTag_default()(HorizontalScroller_module/* default */.Z, HorizontalScroller_module_options);




       /* harmony default export */ const components_HorizontalScroller_module = (HorizontalScroller_module/* default */.Z && HorizontalScroller_module/* default */.Z.locals ? HorizontalScroller_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/components/HorizontalScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */


var HorizontalScroller = /** @class */ (function () {
    function HorizontalScroller(container) {
        this._scroller = this._createScroller(container);
    }
    HorizontalScroller.prototype.scrollTo = function (position, useRem) {
        if (useRem === void 0) { useRem = false; }
        var positionCss;
        if (useRem) {
            positionCss = "".concat(position, "rem");
        }
        else {
            positionCss = "".concat(position, "px");
        }
        lib_addCssStyle(this._scroller, 'transform', "translate(".concat(positionCss, ")"));
        return true;
    };
    HorizontalScroller.prototype._createScroller = function (container) {
        var childNodes = getChildNodes(container);
        var scroller = lib_createElement('div');
        lib_addCssClass(scroller, components_HorizontalScroller_module.mibreit_HorizontalScroller);
        lib_wrapElements(childNodes, scroller);
        return scroller;
    };
    return HorizontalScroller;
}());
/* harmony default export */ const components_HorizontalScroller = (HorizontalScroller);

;// CONCATENATED MODULE: ./src/components/ThumbScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */

var ThumbScroller = /** @class */ (function () {
    function ThumbScroller(layout) {
        this._scrollIndexChangedCallbacks = new Array();
        this._currentScrollIndex = 0;
        this._layout = layout;
        this._scroller = new components_HorizontalScroller(layout.getScrollerContainer());
        console.log('ThumbScroller#constructor - thumbSizeRem = ', this._layout.getThumbSizeRem(), ', numberOfThumbs = ', this._layout.getNumberOfThumbs(), ', numberOfVisibleThumbs = ', this._layout.getNumberOfVisibleThumbs());
    }
    ThumbScroller.prototype.reinitSize = function () {
        this._layout.reinitSize();
        this.scrollTo(this._currentScrollIndex, false);
    };
    ThumbScroller.prototype.scrollTo = function (index, useCenterIndex) {
        var _this = this;
        if (useCenterIndex === void 0) { useCenterIndex = true; }
        var newIndex = index;
        if (useCenterIndex) {
            newIndex -= Math.floor(this._layout.getNumberOfVisibleThumbs() / 2);
        }
        var normalizedIndex = this._normalizeIndex(newIndex);
        console.log('ThumbScroller#scrollTo', newIndex, normalizedIndex);
        this._currentScrollIndex = normalizedIndex;
        var currentScrollPosition = -normalizedIndex * this._layout.getThumbSizeRem();
        this._scroller.scrollTo(currentScrollPosition, true);
        this._scrollIndexChangedCallbacks.forEach(function (callback) {
            callback(_this._currentScrollIndex);
        });
    };
    ThumbScroller.prototype.scrollNext = function () {
        var newIndex = this._currentScrollIndex + this._layout.getNumberOfVisibleThumbs();
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (this._currentScrollIndex === maxPos) {
            newIndex = 0;
        }
        else if (newIndex >= maxPos) {
            newIndex = maxPos;
        }
        this.scrollTo(newIndex, false);
    };
    ThumbScroller.prototype.scrollPrevious = function () {
        var newIndex = this._currentScrollIndex - this._layout.getNumberOfVisibleThumbs();
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (this._currentScrollIndex === 0) {
            newIndex = maxPos;
        }
        else if (newIndex < 0) {
            newIndex = 0;
        }
        this.scrollTo(newIndex, false);
    };
    ThumbScroller.prototype.addScrollIndexChangedCallback = function (callback) {
        if (!this._scrollIndexChangedCallbacks.includes(callback)) {
            this._scrollIndexChangedCallbacks.push(callback);
        }
    };
    ThumbScroller.prototype._normalizeIndex = function (index) {
        var maxPos = this._layout.getNumberOfThumbs() - this._layout.getNumberOfVisibleThumbs();
        if (index >= maxPos) {
            return maxPos;
        }
        else if (index < 0) {
            return 0;
        }
        else {
            return index;
        }
    };
    return ThumbScroller;
}());
/* harmony default export */ const components_ThumbScroller = (ThumbScroller);

;// CONCATENATED MODULE: ./src/containers/ThumbScrollerContainer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */





var DEFAULT_NUMBER_VISIBLE_THUMBS = 7;
var ThumbScrollerContainer = /** @class */ (function () {
    function ThumbScrollerContainer(thumbContainer, thumbElements, config, thumbClickedCallback) {
        this._thumbScroller = null;
        var numberVisibleThumbs = (config === null || config === void 0 ? void 0 : config.numberOfVisibleThumbs)
            ? config.numberOfVisibleThumbs
            : DEFAULT_NUMBER_VISIBLE_THUMBS;
        var thumbs = this._prepareThumbs(thumbElements);
        var lazyLoader = new components_LazyLoader(thumbs, numberVisibleThumbs, numberVisibleThumbs);
        var layout = createThumbScrollerLayout(thumbContainer, thumbs, numberVisibleThumbs, thumbClickedCallback);
        if (numberVisibleThumbs < thumbs.length) {
            this._thumbScroller = this._prepareThumbScroller(layout, lazyLoader, config === null || config === void 0 ? void 0 : config.initialIndex);
            this._addThumbScrollerInteraction(this._thumbScroller, layout);
        }
        else {
            setTimeout(function () {
                lazyLoader.loadElement(0);
                lazyLoader.setCurrentIndex(0);
            }, 0);
        }
    }
    ThumbScrollerContainer.prototype.getScroller = function () {
        return this._thumbScroller;
    };
    ThumbScrollerContainer.prototype._prepareThumbs = function (thumbSelector) {
        var thumbs = new Array();
        for (var i = 0; i < thumbSelector.length; i++) {
            var image = new components_Image(thumbSelector[i]);
            thumbs.push(image);
        }
        return thumbs;
    };
    ThumbScrollerContainer.prototype._prepareThumbScroller = function (layout, loader, initialIndex) {
        if (initialIndex === void 0) { initialIndex = 0; }
        var thumbScroller = new components_ThumbScroller(layout);
        thumbScroller.addScrollIndexChangedCallback(function (index) {
            loader.setCurrentIndex(index);
        });
        thumbScroller.scrollTo(initialIndex);
        return thumbScroller;
    };
    ThumbScrollerContainer.prototype._addThumbScrollerInteraction = function (thumbScroller, thumbScrollerLayout) {
        var _a = thumbScrollerLayout.getThumbScrollerButtons(), previousButton = _a.previousButton, nextButton = _a.nextButton;
        addClickEventListener(nextButton, function () {
            thumbScroller.scrollNext();
        });
        addClickEventListener(previousButton, function () {
            thumbScroller.scrollPrevious();
        });
    };
    return ThumbScrollerContainer;
}());
/* harmony default export */ const containers_ThumbScrollerContainer = (ThumbScrollerContainer);

;// CONCATENATED MODULE: ./src/tools/checkThumbScrollerConfig.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
function checkThumbScrollerConfig_checkConfig(config) {
    if (typeof config.numberOfVisibleThumbs !== 'undefined' &&
        (typeof config.numberOfVisibleThumbs !== 'number' || config.numberOfVisibleThumbs < 0)) {
        throw new Error('checkThumbScrollerConfig - numberOfVisibleThumbs is invalid number');
    }
    if (typeof config.initialIndex !== 'undefined' &&
        (typeof config.initialIndex !== 'number' || config.initialIndex < 0)) {
        throw new Error('checkThumbScrollerConfig - config initialIndex should be of type number');
    }
}

;// CONCATENATED MODULE: ./src/factories/createThumbScroller.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



/* harmony default export */ function createThumbScroller(containerSelector, thumbSelector, config, thumbClickedCallback) {
    if (typeof containerSelector !== 'string') {
        throw new Error('createThumbScroller - first parameter must be containerSelector string');
    }
    if (typeof thumbSelector !== 'string') {
        throw new Error('createThumbScroller - second parameter must be imageSelector string');
    }
    if (config) {
        checkThumbScrollerConfig_checkConfig(config);
    }
    var elements = lib_getElements(thumbSelector);
    var container = getElement(containerSelector);
    if (container && (elements === null || elements === void 0 ? void 0 : elements.length) > 0) {
        return new containers_ThumbScrollerContainer(container, elements, config, thumbClickedCallback).getScroller();
    }
    else {
        throw new Error('createThumbScroller - no images selected');
    }
}

;// CONCATENATED MODULE: ./src/containers/GalleryContainer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
var GalleryContainer = /** @class */ (function () {
    function GalleryContainer(viewer, loader, thumbScroller, fullscreenContainer) {
        this._fullscreenContainer = null;
        this._thumbScroller = null;
        this._viewer = viewer;
        this._loader = loader;
        this._thumbScroller = thumbScroller;
        this._fullscreenContainer = fullscreenContainer;
    }
    GalleryContainer.prototype.getViewer = function () {
        return this._viewer;
    };
    GalleryContainer.prototype.getLoader = function () {
        return this._loader;
    };
    GalleryContainer.prototype.getScroller = function () {
        return this._thumbScroller;
    };
    GalleryContainer.prototype.getFullscreen = function () {
        return this._fullscreenContainer;
    };
    return GalleryContainer;
}());
/* harmony default export */ const containers_GalleryContainer = (GalleryContainer);

// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/containers/FullscreenContainer.module.css
var FullscreenContainer_module = __webpack_require__(799);
;// CONCATENATED MODULE: ./src/containers/FullscreenContainer.module.css

      
      
      
      
      
      
      
      
      

var FullscreenContainer_module_options = {};

;
FullscreenContainer_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      FullscreenContainer_module_options.insert = insertBySelector_default().bind(null, "head");
    
FullscreenContainer_module_options.domAPI = (singletonStyleDomAPI_default());
FullscreenContainer_module_options.insertStyleElement = (insertStyleElement_default());

var FullscreenContainer_module_update = injectStylesIntoStyleTag_default()(FullscreenContainer_module/* default */.Z, FullscreenContainer_module_options);




       /* harmony default export */ const containers_FullscreenContainer_module = (FullscreenContainer_module/* default */.Z && FullscreenContainer_module/* default */.Z.locals ? FullscreenContainer_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/images/close.svg
/* harmony default export */ const images_close = ("<svg viewBox=\"0 0 512 512\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\"><path d=\"M437.5,386.6L306.9,256l130.6-130.6c14.1-14.1,14.1-36.8,0-50.9c-14.1-14.1-36.8-14.1-50.9,0L256,205.1L125.4,74.5  c-14.1-14.1-36.8-14.1-50.9,0c-14.1,14.1-14.1,36.8,0,50.9L205.1,256L74.5,386.6c-14.1,14.1-14.1,36.8,0,50.9  c14.1,14.1,36.8,14.1,50.9,0L256,306.9l130.6,130.6c14.1,14.1,36.8,14.1,50.9,0C451.5,423.4,451.5,400.6,437.5,386.6z\"/></svg>");
;// CONCATENATED MODULE: ./src/containers/FullscreenContainer.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



var GALLERY_PLACEHOLDER_ID = 'slideshowContainerPlaceholder';
var THUMBS_PLACEHOLDER_ID = 'thumbContainerPlaceholder';
var FullscreenContainer = /** @class */ (function () {
    function FullscreenContainer(slideshowContainer, thumbContainer, usePlaceholder) {
        if (thumbContainer === void 0) { thumbContainer = null; }
        if (usePlaceholder === void 0) { usePlaceholder = true; }
        this._fullscreenChangedCallbacks = [];
        this._thumbContainer = null;
        this._thumbContainerPlaceholder = null;
        this._fullscreenActive = false;
        this._slideshowContainer = slideshowContainer;
        this._thumbContainer = thumbContainer;
        this._usePlaceholder = usePlaceholder;
        this._fullScreenContainer = this._createFullscreenContainer();
        this._fullScreenCloseButton = this._createFullscreenCloseButton(this._fullScreenContainer);
        this._slideshowContainerPlaceholder = this._createSlideshowContainerPlaceholder();
        if (thumbContainer) {
            this._thumbContainerPlaceholder = this._createThumbContainerPlaceholder();
        }
    }
    FullscreenContainer.prototype.activate = function () {
        if (!this._fullscreenActive) {
            this._moveGalleryToFullscreen();
            this._moveThumbsToFullscreen();
            this._addFullscreen();
            this._setupCloseButtonHandler();
            this._fullscreenActive = true;
            this._fullscreenChangedCallbacks.forEach(function (callback) {
                callback(true);
            });
        }
    };
    FullscreenContainer.prototype.deActivate = function () {
        if (this._fullscreenActive) {
            this._removeGalleryFromFullscreen();
            this._removeThumbsFromFullscreen();
            this._removeFullscreen();
            this._fullscreenActive = false;
            this._fullscreenChangedCallbacks.forEach(function (callback) {
                callback(false);
            });
        }
    };
    FullscreenContainer.prototype.addFullscreenChangedCallback = function (callback) {
        if (!this._fullscreenChangedCallbacks.includes(callback)) {
            this._fullscreenChangedCallbacks.push(callback);
        }
    };
    FullscreenContainer.prototype.isFullscreenActive = function () {
        return this._fullscreenActive;
    };
    FullscreenContainer.prototype.setBackgroundColor = function (color) {
        lib_addCssStyle(this._fullScreenContainer, 'background-color', color);
    };
    FullscreenContainer.prototype._createFullscreenContainer = function () {
        var fullScreenContainer = lib_createElement('div');
        lib_addCssClass(fullScreenContainer, containers_FullscreenContainer_module.mibreit_Fullscreen);
        return fullScreenContainer;
    };
    FullscreenContainer.prototype._createFullscreenCloseButton = function (fullScreenContainer) {
        var fullScreenCloseButton = lib_createElement('div');
        setInnerHtml(fullScreenCloseButton, images_close);
        lib_addCssClass(fullScreenCloseButton, containers_FullscreenContainer_module.mibreit_Fullscreen_exit);
        appendChildElement(fullScreenCloseButton, fullScreenContainer);
        return fullScreenCloseButton;
    };
    FullscreenContainer.prototype._createSlideshowContainerPlaceholder = function () {
        var slideshowContainerPlaceholder = lib_createElement('div');
        setAttribute(slideshowContainerPlaceholder, 'id', GALLERY_PLACEHOLDER_ID);
        return slideshowContainerPlaceholder;
    };
    FullscreenContainer.prototype._createThumbContainerPlaceholder = function () {
        var thumbContainerPlaceholder = lib_createElement('div');
        setAttribute(thumbContainerPlaceholder, 'id', THUMBS_PLACEHOLDER_ID);
        return thumbContainerPlaceholder;
    };
    FullscreenContainer.prototype._addFullscreen = function () {
        var body = getElement('body');
        if (body != null) {
            appendChildElement(this._fullScreenContainer, body);
        }
    };
    FullscreenContainer.prototype._removeFullscreen = function () {
        lib_removeElement(this._fullScreenContainer);
    };
    FullscreenContainer.prototype._moveGalleryToFullscreen = function () {
        if (this._usePlaceholder) {
            prependBeforeChild(this._slideshowContainerPlaceholder, this._slideshowContainer);
        }
        appendChildElement(this._slideshowContainer, this._fullScreenContainer);
        lib_addCssStyle(this._slideshowContainer, 'width', '100%');
        lib_addCssStyle(this._slideshowContainer, 'flex-grow', '1');
    };
    FullscreenContainer.prototype._removeGalleryFromFullscreen = function () {
        lib_removeElement(this._slideshowContainer);
        if (this._usePlaceholder) {
            prependBeforeChild(this._slideshowContainer, this._slideshowContainerPlaceholder);
            lib_removeElement(this._slideshowContainerPlaceholder);
        }
        removeCssStyle(this._slideshowContainer, 'width');
        removeCssStyle(this._slideshowContainer, 'flex-grow');
    };
    FullscreenContainer.prototype._moveThumbsToFullscreen = function () {
        if (this._thumbContainer && this._thumbContainerPlaceholder) {
            if (this._usePlaceholder) {
                prependBeforeChild(this._thumbContainerPlaceholder, this._thumbContainer);
            }
            appendChildElement(this._thumbContainer, this._fullScreenContainer);
            lib_addCssStyle(this._thumbContainer, 'flex-grow', '0');
        }
    };
    FullscreenContainer.prototype._removeThumbsFromFullscreen = function () {
        if (this._thumbContainer && this._thumbContainerPlaceholder) {
            lib_removeElement(this._thumbContainer);
            if (this._usePlaceholder) {
                prependBeforeChild(this._thumbContainer, this._thumbContainerPlaceholder);
                lib_removeElement(this._thumbContainerPlaceholder);
            }
            removeCssStyle(this._thumbContainer, 'flex-grow');
        }
    };
    FullscreenContainer.prototype._setupCloseButtonHandler = function () {
        var _this = this;
        addClickEventListener(this._fullScreenCloseButton, function (event) {
            event === null || event === void 0 ? void 0 : event.stopPropagation();
            if (_this._fullscreenActive) {
                _this.deActivate();
            }
        });
    };
    return FullscreenContainer;
}());
/* harmony default export */ const containers_FullscreenContainer = (FullscreenContainer);

;// CONCATENATED MODULE: ./src/images/nextImage.svg
/* harmony default export */ const nextImage = ("<svg viewBox=\"0 0 1792 1792\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M1363 877l-742 742q-19 19-45 19t-45-19l-166-166q-19-19-19-45t19-45l531-531-531-531q-19-19-19-45t19-45l166-166q19-19 45-19t45 19l742 742q19 19 19 45t-19 45z\"/></svg>");
;// CONCATENATED MODULE: ./src/images/fullscreen.svg
/* harmony default export */ const fullscreen = ("<svg viewBox=\"0 0 14 14\" xmlns=\"http://www.w3.org/2000/svg\" xmlns:xlink=\"http://www.w3.org/1999/xlink\"><path d=\"M2,9 L0,9 L0,14 L5,14 L5,12 L2,12 L2,9 L2,9 Z M0,5 L2,5 L2,2 L5,2 L5,0 L0,0 L0,5 L0,5 Z M12,12 L9,12 L9,14 L14,14 L14,9 L12,9 L12,12 L12,12 Z M9,0 L9,2 L12,2 L12,5 L14,5 L14,0 L9,0 L9,0 Z\"/></svg>");
// EXTERNAL MODULE: ./node_modules/css-loader/dist/cjs.js??ruleSet[1].rules[1].use[1]!./src/builders/GalleryContainerBuilder.module.css
var GalleryContainerBuilder_module = __webpack_require__(393);
;// CONCATENATED MODULE: ./src/builders/GalleryContainerBuilder.module.css

      
      
      
      
      
      
      
      
      

var GalleryContainerBuilder_module_options = {};

;
GalleryContainerBuilder_module_options.setAttributes = (setAttributesWithoutAttributes_default());

      GalleryContainerBuilder_module_options.insert = insertBySelector_default().bind(null, "head");
    
GalleryContainerBuilder_module_options.domAPI = (singletonStyleDomAPI_default());
GalleryContainerBuilder_module_options.insertStyleElement = (insertStyleElement_default());

var GalleryContainerBuilder_module_update = injectStylesIntoStyleTag_default()(GalleryContainerBuilder_module/* default */.Z, GalleryContainerBuilder_module_options);




       /* harmony default export */ const builders_GalleryContainerBuilder_module = (GalleryContainerBuilder_module/* default */.Z && GalleryContainerBuilder_module/* default */.Z.locals ? GalleryContainerBuilder_module/* default */.Z.locals : undefined);

;// CONCATENATED MODULE: ./src/tools/debounce.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */
/* harmony default export */ function tools_debounce(callback, wait, smooth) {
    if (smooth === void 0) { smooth = true; }
    var timeout;
    var waiting = false;
    if (smooth) {
        return function debouncedCallback() {
            if (!waiting) {
                var later = function () {
                    waiting = false;
                    clearTimeout(timeout);
                    callback();
                };
                waiting = true;
                timeout = window.setTimeout(later, wait);
            }
        };
    }
    else {
        return function debouncedCallback() {
            var later = function () {
                clearTimeout(timeout);
                callback();
            };
            clearTimeout(timeout);
            timeout = window.setTimeout(later, wait);
        };
    }
}

;// CONCATENATED MODULE: ./src/builders/GalleryContainerBuilder.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */





// images


// styles


// constants



var RESIZE_DEBOUNCE_TIMER = 500;
var GalleryContainerBuilder = /** @class */ (function () {
    function GalleryContainerBuilder(container, images, config) {
        this._thumbContainer = null;
        this._fullscreenButton = null;
        this._thumbScroller = null;
        this._fullscreenContainer = null;
        this._container = container;
        var slideshowContainer = new containers_SlideshowContainer(images, config);
        this._viewer = slideshowContainer.getViewer();
        this._loader = slideshowContainer.getLoader();
        var _a = this._createPreviousNextButtons(container), previousButton = _a.previousButton, nextButton = _a.nextButton;
        this._setupHoverEvents(container, [previousButton, nextButton]);
        this._setupSwipeHandler(container, this._viewer);
    }
    GalleryContainerBuilder.prototype.addThumbScroller = function (thumbContainer, thumbs, config) {
        var _this = this;
        this._thumbContainer = thumbContainer;
        this._thumbScroller = new containers_ThumbScrollerContainer(thumbContainer, thumbs, config, function (index) {
            _this._loader.setCurrentIndex(index);
            _this._viewer.showImage(index);
        }).getScroller();
        if (this._thumbScroller) {
            this._viewer.addImageChangedCallback(function (index, _imageInfo) {
                _this._thumbScroller.scrollTo(index, true);
            });
        }
        return this;
    };
    GalleryContainerBuilder.prototype.build = function () {
        this._fullscreenContainer = new containers_FullscreenContainer(this._container, this._thumbContainer);
        this._fullscreenButton = this._createFullscreenButton(this._container);
        this._setupHoverEvents(this._container, [this._fullscreenButton]);
        this._setupKeyEvents(this._viewer, this._fullscreenContainer);
        this._setupFullscreenClickEvent(this._fullscreenButton, this._fullscreenContainer, this._viewer);
        this._setupResizeHandler(this._viewer, this._thumbScroller);
        if (this._fullscreenContainer && this._fullscreenButton) {
            this._setupFullscreenChangedHandler(this._fullscreenContainer, this._fullscreenButton, this._viewer, this._thumbScroller);
        }
        return new containers_GalleryContainer(this._viewer, this._loader, this._thumbScroller, this._fullscreenContainer);
    };
    GalleryContainerBuilder.prototype._createPreviousNextButtons = function (container) {
        var previousButton = lib_createElement('div');
        setInnerHtml(previousButton, nextImage);
        lib_addCssClass(previousButton, builders_GalleryContainerBuilder_module.mibreit_GalleryPrevious);
        lib_addCssClass(previousButton, tools_animations_module.mibreit_GalleryFade);
        prependChildElement(previousButton, container);
        var nextButton = lib_createElement('div');
        setInnerHtml(nextButton, nextImage);
        lib_addCssClass(nextButton, builders_GalleryContainerBuilder_module.mibreit_GalleryNext);
        lib_addCssClass(nextButton, tools_animations_module.mibreit_GalleryFade);
        appendChildElement(nextButton, container);
        return { previousButton: previousButton, nextButton: nextButton };
    };
    GalleryContainerBuilder.prototype._setupSwipeHandler = function (container, imageViewer) {
        lib_addCssStyle(container, 'touch-action', 'pinch-zoom pan-y');
        new SwipeHandler(container, function (direction, position) {
            var containerWidth = lib_getElementDimension(container).width;
            var containerPosX = getElementPosition(container).x;
            if (direction === ESwipeDirection.LEFT) {
                imageViewer.showPreviousImage(direction);
            }
            else if (direction === ESwipeDirection.RIGHT) {
                imageViewer.showNextImage(direction);
            }
            else {
                if (position.x - containerPosX > containerWidth / 2) {
                    imageViewer.showNextImage();
                }
                else {
                    imageViewer.showPreviousImage();
                }
            }
        });
    };
    GalleryContainerBuilder.prototype._setupResizeHandler = function (imageViewer, thumbScroller) {
        if (thumbScroller) {
            var debouncedThumbResizer_1 = tools_debounce(function () {
                thumbScroller.reinitSize();
            }, RESIZE_DEBOUNCE_TIMER, false);
            addResizeEventListener(function () {
                imageViewer.reinitSize();
                debouncedThumbResizer_1();
            });
        }
        else {
            addResizeEventListener(function () {
                imageViewer.reinitSize();
            });
        }
    };
    GalleryContainerBuilder.prototype._createFullscreenButton = function (container) {
        var fullscreenButton = lib_createElement('div');
        setInnerHtml(fullscreenButton, fullscreen);
        lib_addCssClass(fullscreenButton, builders_GalleryContainerBuilder_module.mibreit_GalleryFullscreenButton);
        lib_addCssClass(fullscreenButton, tools_animations_module.mibreit_GalleryFade);
        appendChildElement(fullscreenButton, container);
        return fullscreenButton;
    };
    GalleryContainerBuilder.prototype._setupHoverEvents = function (container, buttons) {
        addEventListener(container, 'mouseenter', function () {
            buttons.forEach(function (button) {
                lib_addCssStyle(button, 'opacity', "".concat(GALLERY_BUTTONS_SHOW_OPACITY));
            });
        });
        addEventListener(container, 'mouseleave', function () {
            buttons.forEach(function (button) {
                lib_addCssStyle(button, 'opacity', '0');
            });
        });
    };
    GalleryContainerBuilder.prototype._setupKeyEvents = function (imageViewer, fullScreen) {
        addKeyEventListener(function (event) {
            var key = getKeyFromEvent(event);
            switch (key) {
                case 'ArrowRight':
                    imageViewer.showNextImage();
                    break;
                case 'ArrowLeft':
                    imageViewer.showPreviousImage();
                    break;
                case 'Escape':
                    fullScreen.deActivate();
                    break;
                case 'f':
                    if (fullScreen.isFullscreenActive()) {
                        fullScreen.deActivate();
                    }
                    else {
                        fullScreen.activate();
                    }
                    break;
                default:
                    if (fullScreen.isFullscreenActive()) {
                        fullScreen.deActivate();
                    }
                    break;
            }
        });
    };
    GalleryContainerBuilder.prototype._setupFullscreenClickEvent = function (fullscreenButton, fullScreen, imageViewer) {
        addEventListener(fullscreenButton, 'pointerdown', function (event) {
            event.stopPropagation();
        });
        addEventListener(fullscreenButton, 'pointerup', function (event) {
            event.stopPropagation();
            if (!fullScreen.isFullscreenActive()) {
                fullScreen.activate();
                imageViewer.reinitSize();
                lib_addCssStyle(fullscreenButton, 'display', 'none');
            }
        });
    };
    GalleryContainerBuilder.prototype._setupFullscreenChangedHandler = function (FullscreenContainer, fullscreenButton, imageViewer, thumbScroller) {
        FullscreenContainer.addFullscreenChangedCallback(function (active) {
            if (thumbScroller) {
                thumbScroller.reinitSize();
            }
            imageViewer.reinitSize();
            if (active) {
                lib_addCssStyle(fullscreenButton, 'display', 'none');
            }
            else {
                removeCssStyle(fullscreenButton, 'display');
            }
        });
    };
    return GalleryContainerBuilder;
}());
/* harmony default export */ const builders_GalleryContainerBuilder = (GalleryContainerBuilder);

;// CONCATENATED MODULE: ./src/factories/createGallery.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */




/* harmony default export */ function createGallery(containerSelector, imageSelector, config) {
    if (typeof containerSelector !== 'string') {
        throw new Error('createGallery - first parameter must be containerSelector string');
    }
    if (typeof imageSelector !== 'string') {
        throw new Error('createGallery - second parameter must be imageSelector string');
    }
    if (config) {
        checkSlideshowConfig(config);
    }
    var elements = lib_getElements(imageSelector);
    var container = getElement(containerSelector);
    if (container && (elements === null || elements === void 0 ? void 0 : elements.length) > 0) {
        var builder = new builders_GalleryContainerBuilder(container, elements, config);
        if (typeof (config === null || config === void 0 ? void 0 : config.thumbSelector) === 'string' && typeof (config === null || config === void 0 ? void 0 : config.thumbContainerSelector) === 'string') {
            checkThumbScrollerConfig_checkConfig(config);
            var thumbContainer = getElement(config.thumbContainerSelector);
            var thumbs = lib_getElements(config.thumbSelector);
            if (thumbContainer && thumbs) {
                if (thumbs.length > 1) {
                    builder.addThumbScroller(thumbContainer, thumbs, config);
                }
                else {
                    lib_addCssStyle(thumbContainer, 'display', 'none');
                }
            }
        }
        return builder.build();
    }
    else {
        throw new Error('createGallery - no images selected');
    }
}

;// CONCATENATED MODULE: ./src/factories/createFullscreenOnlyGallery.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */



var IMAGES_CONTAINER_CLASS = 'mibreit_gallery_fullscreen_only_container';
function createImagesContainer() {
    var container = lib_createElement('div');
    lib_addCssStyle(container, 'display', 'none');
    lib_addCssClass(container, IMAGES_CONTAINER_CLASS);
    var body = getElement('body');
    appendChildElement(container, body);
    return container;
}
/* harmony default export */ function createFullscreenOnlyGallery(imageSelector, config) {
    if (typeof imageSelector !== 'string') {
        throw new Error('createFullscreenOnlyGallery - first parameter must be imageSelector string');
    }
    checkSlideshowConfig(config);
    var elements = lib_getElements(imageSelector);
    if ((elements === null || elements === void 0 ? void 0 : elements.length) > 0) {
        var container_1 = createImagesContainer();
        elements.forEach(function (image) {
            appendChildElement(cloneElement(image), container_1);
        });
        var clonedElements = lib_getElements(".".concat(IMAGES_CONTAINER_CLASS, " > img"));
        var builder = new builders_GalleryContainerBuilder(container_1, clonedElements, config);
        var galleryContainer = builder.build();
        var fullscreen = galleryContainer.getFullscreen();
        var viewer_1 = galleryContainer.getViewer();
        if (fullscreen && viewer_1) {
            fullscreen.addFullscreenChangedCallback(function (active) {
                if (active) {
                    removeCssStyle(container_1, 'display');
                    viewer_1.reinitSize();
                }
                else {
                    lib_addCssStyle(container_1, 'display', 'none');
                }
            });
        }
        return galleryContainer;
    }
    else {
        throw new Error('createFullscreenOnlyGallery - no images selected');
    }
}

;// CONCATENATED MODULE: ./src/index.ts
/**
 * @author Michael Breitung
 * @copyright Michael Breitung Photography (www.mibreit-photo.com)
 */






})();

var __webpack_export_target__ = exports;
for(var i in __webpack_exports__) __webpack_export_target__[i] = __webpack_exports__[i];
if(__webpack_exports__.__esModule) Object.defineProperty(__webpack_export_target__, "__esModule", { value: true });
/******/ })()
;